# 📗 دستور عمل کامل — فایننس‌پرو (FinancePro)

> **دفترکل دیجیتال شخصی برای کاربران فارسی‌زبان** — تقویم شمسی، اعداد فارسی، ربات تلگرام، همگام‌سازی ابری
>
> این سند همهٔ کارهایی را که قدم‌به‌قدم انجام شده تا پروژه به وضعیت فعلی برسد ثبت می‌کند؛ تا هر توسعه‌دهنده (یا خودِ شما در آینده) بداند **چه چیزی، چرا و چگونه** ساخته شده است.
> آخرین به‌روزرسانی: پس از افزودن «گزارش هوشمند» و اصلاح کامل دستیار صوتی.

---

## فهرست مطالب

1. [معرفی پروژه](#۱-معرفی-پروژه)
2. [پشتهٔ فناوری](#۲-پشته-فناوری)
3. [اجرا، استقرار و تاریخچهٔ دیپلوی](#۳-اجرا-استقرار-و-تاریخچه-دیپلوی)
4. [ساختار فعلی فایل‌ها](#۴-ساختار-فعلی-فایل‌ها)
5. [فهرست کامل قابلیت‌های پیاده‌شده](#۵-فهرست-کامل-قابلیت‌های-پیاده‌شده)
6. [مدل داده](#۶-مدل-داده)
7. [معماری همگام‌سازی و چنددستگاهی](#۷-معماری-همگام‌سازی-و-چنددستگاهی)
8. [PWA و نصب روی گوشی](#۸-pwa-و-نصب-روی-گوشی)
9. [دستیار صوتی](#۹-دستیار-صوتی)
10. [تشخیص هوشمند و تگ‌های تراکنش](#۱۰-تشخیص-هوشمند-و-تگ‌های-تراکنش)
11. [تم‌ها و ظاهر](#۱۱-تم‌ها-و-ظاهر)
12. [درس‌های فنی و نکات مهم توسعه](#۱۲-درس‌های-فنی-و-نکات-مهم-توسعه)
13. [کارهای باقی‌مانده و ایده‌های آینده](#۱۳-کارهای-باقی‌مانده-و-ایده‌های-آینده)
14. [لاگ تغییرات (پس از انتشار نسخهٔ ۱)](#۱۴-لاگ-تغییرات-پس-از-انتشار-نسخه-۱)

---

## ۱. معرفی پروژه

فایننس‌پرو یک وب‌اپلیکیشن مدیریت مالی شخصی برای کاربران ایرانی است. برنامه از یک «سند تحویل پروژه» شروع شد و مرحله‌به‌مرحله به یک محصول کامل تبدیل شد.

**اصول حاکم بر کل برنامه:**

| اصل | توضیح |
|---|---|
| کاملاً راست‌به‌چپ و فارسی | تمام رابط، فونت‌ها (Lalezar + Vazirmatn) و اعداد فارسی |
| ذخیره میلادی، نمایش شمسی | همهٔ تاریخ‌ها ISO ذخیره و فقط هنگام نمایش با jalaali-js شمسی می‌شوند |
| اعداد فارسی با جداکنندهٔ هزارگان | `faNum()` و `faMoney()` در همه‌جا؛ ورودی مبلغ هم هنگام تایپ جداکننده می‌گیرد |
| چندکاربره | هر کاربر دادهٔ کاملاً جدا؛ ورود یکسان در همهٔ دستگاه‌ها |
| حذف امن | هیچ‌چیز فوراً پاک نمی‌شود — سطل ۳۰ ثانیه‌ای با نوار بازگردانی |
| ابر + آفلاین | داده هم در مرورگر (localStorage) و هم در Supabase؛ بدون اینترنت هم کار می‌کند |

---

## ۲. پشتهٔ فناوری

| حوزه | فناوری |
|---|---|
| فریم‌ورک | React 18 + Vite 6 |
| زبان | TypeScript |
| استایل | Tailwind CSS v4 + متغیرهای `--fp-*` برای تم |
| فونت | Lalezar (تیتر) + Vazirmatn (متن) |
| تقویم شمسی | jalaali-js |
| نمودار | Recharts |
| آیکن | Lucide React |
| خروجی اکسل | ExcelJS (خروجی ۸ برگی استایل‌دار) |
| دیتابیس/بک‌اند | Supabase (PostgreSQL + REST) |
| میزبانی | Vercel (استاتیک + دیپلوی خودکار از GitHub) |
| تشخیص گفتار | Web Speech API (زبان `fa-IR`) |
| اعلان پس‌زمینه | Service Worker + Notification API + WebAudio |

---

## ۳. اجرا، استقرار و تاریخچهٔ دیپلوی

### ۳٫۱. اجرای لوکال

```bash
npm install        # فقط بار اول
npm run dev        # → http://localhost:3000
npm run build      # بیلد تولید → پوشهٔ dist
```

### ۳٫۲. مراحل استقرار (همان‌طور که با هم انجام شد)

| مرحله | کار | وضعیت |
|---|---|---|
| ۱ | ساخت پروژهٔ رایگان در supabase.com | ✅ انجام شد |
| ۲ | اجرای SQL ساخت جدول `financepro_state` در SQL Editor | ✅ انجام شد |
| ۳ | برداشتن آدرس پروژه و کلید `anon` از Settings → API | ✅ انجام شد |
| ۴ | ایمپورت ریپازیتوری GitHub در Vercel و دیپلوی | ✅ انجام شد |
| ۵ | اتصال سایت به Supabase از تنظیمات + تست ماندگاری | ✅ انجام شد |
| ۶ | اجرای SQL ساخت جدول `fp_users` (ورود چنددستگاهی) | ✅ انجام شد |
| ۷ | افزودن متغیرهای محیطی `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY` در Vercel + Redeploy | ✅ انجام شد |

### ۳٫۳. جدول‌های ساخته‌شده در Supabase

```sql
-- دفترکل مشترک سایت و ربات (هر کاربر یک ردیف با شناسهٔ fp-user-<نام‌کاربری>)
create table if not exists public.financepro_state (
  id         text primary key,
  data       text,
  updated_at timestamptz default now()
);
alter table public.financepro_state enable row level security;
create policy "open_anon" on public.financepro_state for all using (true) with check (true);

-- حساب‌های کاربران برای ورود یکسان در همهٔ دستگاه‌ها
create table if not exists public.fp_users (
  username   text primary key,
  data       text,
  updated_at timestamptz default now()
);
alter table public.fp_users enable row level security;
create policy "open_anon" on public.fp_users for all using (true) with check (true);
```

> ⚠️ **هشدار امنیتی:** کلید `anon` برای استفادهٔ عمومی طراحی شده و در کدِ فرانت مشکلی ندارد، اما کلید `service_role` را هرگز جایی نگذارید. برای تولید، policy ها را محدود به کاربر مشخص کنید.

### ۳٫۴. مشکلات دیپلوی که حل شدند (درس‌های Vercel)

| مشکل | علامت | راه‌حل |
|---|---|---|
| نصب نشدن وابستگی‌ها | `vite: command not found` | فیلد **Install Command** را روی `npm install` گذاشتیم |
| جای اشتباه فرمان‌ها | `Running "install" command: npm run build` | Install = `npm install`، Build = `npm run build` — جای این دو عوض شده بود |
| package.json پیدا نشد | `ENOENT /vercel/path0/package.json` | ساختار تودرتو بود → نام پوشهٔ سطح اول را در **Root Directory** نوشتیم |
| خطای PostCSS لوکال | `Cannot find module '@tailwindcss/postcss'` | یک فایل `postcss.config.mjs` مزاحم در پوشهٔ *بالاتر* (Downloads) بود که ویت پیدایش می‌کرد. با ساخت `postcss.config.mjs` خالی داخل پروژه (`export default { plugins: {} };`) مشکل برای همیشه حل شد |

> 🔑 **قانون طلایی ماندگاری:** داده‌ها **هرگز** روی سرور Vercel نیستند؛ در مرورگر کاربر و در Supabase ذخیره می‌شوند. پس هیچ دیپلوی جدیدی نمی‌تواند داده‌ای را پاک کند.

### ۳٫۵. چرخهٔ انتشار بعدی

```bash
git add .
git commit -m "توضیح تغییر"
git push          # → Vercel خودکار دیپلوی می‌کند (۲ تا ۴ دقیقه)
```

---

## ۴. ساختار فعلی فایل‌ها

```
financepro/
├── index.html                ← RTL، فونت‌ها، تم اولیه قبل از رندر
├── postcss.config.mjs        ← خالی — جلوگیری از خوانده‌شدن تنظیمات مزاحمِ بیرونی
├── DASTOOR-AMAL.md           ← همین سند
├── public/
│   ├── manifest.webmanifest  ← شناسنامهٔ PWA
│   ├── icon.svg              ← آیکون برنامه
│   └── sw.js                 ← Service Worker (کش آفلاین + اعلان پس‌زمینه)
└── src/
    ├── main.tsx              ← بوت‌استرپ + ثبت Service Worker
    ├── App.tsx               ← پوسته: ورود/ثبت‌نام، ناوبری، قفل پین، نوار بازگشت،
    │                            زنگ قرارها، سینک خودکار، دکمهٔ نصب PWA، تم‌ها
    ├── index.css             ← سیستم طراحی: متغیرهای --fp-*، تم روشن/تیره، ۶ تم رنگی
    ├── ui.tsx                ← کتابخانهٔ UI: توست، مودال، تقویم شمسی، فرم‌ها،
    │                            MicButton (دستیار صوتی)، EditBtn/DeleteBtn
    ├── pages.tsx             ← داشبورد، تراکنش‌ها، گزارش دسته‌ها، بدهی‌ها + فرم تراکنش
    ├── pages2.tsx            ← قرارها، یادداشت‌ها، گزارش‌ها (با گزارش هوشمند)، مدیریت، تنظیمات
    ├── widgets.tsx           ← اسپارک‌لاین، نقشهٔ حرارتی، پیش‌بینی، امتیاز سلامت، نشان‌ها
    ├── excel.ts              ← خروجی اکسل ۸ برگی حرفه‌ای + CSV + ICS
    ├── vite-env.d.ts
    └── lib/
        ├── data.tsx          ← مدل داده (۲۰ جدول) + Store مرکزی (mutate/trashItem/restore)
        │                        + تشخیص هوشمند + تگ‌ها + دادهٔ نمونه/پاک‌سازی
        ├── auth.ts           ← احراز هویت چندنفره + ورود از ابر (fp_users)
        ├── cloud.ts          ← سینک Supabase + کد انتقال بین مرورگرها
        ├── themes.ts         ← ۶ تم رنگی ترکیبی
        └── utils.ts          ← تقویم شمسی، اعداد فارسی، بازه‌های زمانی، EMI، زنگ صوتی،
                                 اعلان، کپی، دانلود، هوک‌ها
```

---

## ۵. فهرست کامل قابلیت‌های پیاده‌شده

### 🔐 احراز هویت و چندکاربری
- [x] صفحهٔ ورود/ثبت‌نام (نام، نام کاربری، رمز با هش)
- [x] حالت مهمان بدون ثبت‌نام
- [x] دادهٔ کاملاً جدا برای هر کاربر (`fp_data_<شناسه>`)
- [x] **ورود یکسان در همهٔ دستگاه‌ها** — حساب‌ها در جدول `fp_users` ذخیره می‌شوند؛ مرورگر ناشناس حساب را از ابر می‌خواند
- [x] باکس «اتصال ابری» در خودِ صفحهٔ ورود (برای شکستن بن‌بست تنظیمات قبل از ورود)
- [x] فهرست کاربران دستگاه در تنظیمات با نشان «فعال»
- [x] قفل پین با صفحهٔ بازگشایی
- [x] حذف حساب کاربری

### 📊 داشبورد
- [x] موجودی کل / درآمد ماه / هزینهٔ ماه — **هرکدام با دکمهٔ مخفی‌سازی جداگانه** و شمارش متحرک
- [x] اسپارک‌لاین روند ۳۰ روزهٔ تراز
- [x] موجودی حساب‌ها با **دکمهٔ مخفی‌سازی مستقل**
- [x] فعالیت اخیر (با نشان تراکنش‌های ربات تلگرام)
- [x] چالش پس‌انداز، ارز خارجی، اشتراک‌ها

### 💸 تراکنش‌ها
- [x] ثبت تراکنش **بدون فیلد عنوان** — تراکنش با نام دسته ثبت می‌شود + فیلد «توضیحات» جدا
- [x] **تشخیص هوشمند** به‌صورت کلید روشن/خاموش (مبلغ با «هزار/میلیون/میلیارد»، دسته، کارت بانکی، نوع درآمد/هزینه) — یادش می‌ماند
- [x] **تگ‌های تراکنش:** ضروری / تفریحی / میشد بعدا هم خرید / معمولی و قیمتش کم بود خریدم
- [x] **فیلتر تگ** در لیست تراکنش‌ها + نشان رنگی تگ روی هر ردیف
- [x] ۹ فیلتر زمانی (امروز، دیروز، این هفته، این ماه، ماه قبل، ۳ ماه، ۶ ماه، امسال، همه)
- [x] فیلتر نوع (درآمد/هزینه) + جست‌وجو در توضیحات و دسته‌ها
- [x] گروه‌بندی روزانه با «خالص روز»
- [x] **حذف با بازگشت ۳۰ ثانیه** — نوار بازگردانی با شمارش معکوس زنده
- [x] ویرایش با دکمه‌های پررنگ و همیشه‌نما
- [x] ورود و خروج CSV

### 🥧 گزارش دسته‌ها
- [x] نمودار دونات + نوارهای متحرک سهم هر دسته
- [x] ۹ فیلتر زمانی
- [x] **کلیک روی هر دسته → تراکنش‌های همان دسته در همین صفحه باز می‌شود** (بر اساس فیلتر انتخابی)

### ⚖️ بدهی‌ها
- [x] سه تب: بدهی‌ها / طلب‌ها / اقساط
- [x] تسویهٔ تدریجی + نوار پیشرفت
- [x] **ویرایش و حذف بدهی و طلب**
- [x] ماشین‌حساب وام (EMI)
- [x] QR درخواست وجه

### 📅 قرارها
- [x] تقویم شمسی با نقطهٔ رویدادها
- [x] **انتخابگر ساعت:** شبکهٔ ۲۴تایی ساعت + شبکهٔ ۵ دقیقه‌ای + گزینه‌های سریع (الان، ۳۰ دقیقه دیگر، فردا ۹ صبح…) + نمایش زندهٔ ساعت
- [x] دیکتهٔ صوتی برای عنوان
- [x] خروجی ICS برای هر قرار
- [x] یادآوری یک‌ساعت‌قبل (توست)
- [x] **زنگ سرِ ساعت:** اعلان صوتی (WebAudio) + اعلان سیستم از طریق Service Worker — حتی اگر برنامه در پس‌زمینه باشد؛ کلید فعال‌سازی در بالای صفحه با «تست زنگ»

### 📝 یادداشت‌ها (صفحهٔ جدید)
- [x] ثبت یادداشت با **عنوان + تاریخ شمسی + متن + رنگ** (۵ رنگ)
- [x] نمایش به‌صورت برگه‌های چسبان (Sticky Note) با پونز
- [x] **دیکتهٔ صوتی برای متن** — به متن موجود اضافه می‌کند
- [x] ویرایش و حذف (با سطل ۳۰ ثانیه‌ای)
- [x] جست‌وجو در عنوان و متن

### 📈 گزارش‌ها
- [x] **۹ فیلتر زمانی** (همان فیلترهای تراکنش‌ها) — همهٔ بخش‌ها بر اساس بازه
- [x] نمودار میله‌ای ۶ ماهه، پیش‌بینی ماه بعد، امتیاز سلامت مالی، نقشهٔ حرارتی ۱۴ هفته، نشان‌ها
- [x] **گزارش هوشمند:** دکمه‌ای که یک گزارش متنی کامل و ساختاریافته از همهٔ بخش‌ها می‌سازد — آمادهٔ کپی برای ChatGPT/Claude تا رفتار مالی را تحلیل کند و راهکار بدهد
- [x] **خروجی اکسل حرفه‌ای** — ۸ برگهٔ استایل‌دار (خلاصه، تراکنش‌ها با جمع و فیلتر خودکار، دسته‌ها، حساب‌ها، بدهی‌ها، اقساط، بودجه‌ها، دارایی‌ها) — بر اساس فیلتر زمانی انتخابی
- [x] چاپ / PDF

### 🧰 مدیریت
- [x] CRUD کامل ۱۲ ابزار: حساب، دسته، انتقال، دوره‌ای، هدف، بودجه، روش پرداخت، چک، اشتراک، دارایی، چالش، ارز
- [x] **کارت «داده‌های نمونه»:** دکمهٔ ورود دادهٔ نمونه (چند ماه تراکنش و بدهی و بودجه و هدف و یادداشت) و دکمهٔ «پاک‌سازی همهٔ داده‌ها» با تأیید
- [x] برنامهٔ جدید **کاملاً خالی** شروع می‌شود — دادهٔ نمونه فقط با درخواست کاربر

### ⚙️ تنظیمات
- [x] تم روشن/تیره + **۶ تم رنگی ترکیبی** (سواچ + دکمهٔ پالت در نوار بالا)
- [x] امنیت پین
- [x] همگام‌سازی ابری: ذخیرهٔ آدرس و کلید، سینک دستی، نمایش تنظیمات فعال (دستی یا محیطی)
- [x] **کد انتقال بین مرورگرها** (بدون نیاز به Supabase)
- [x] پشتیبان‌گیری و بازیابی JSON
- [x] راهنمای دستورات ربات تلگرام
- [x] مدیریت حساب کاربری + فهرست کاربران

---

## ۶. مدل داده

۲۰ جدول در `src/lib/data.tsx` (فیلد `notes` به‌تازگی اضافه شد):

| گروه | جدول‌ها |
|---|---|
| هسته | accounts، categories، transactions، transfers |
| مالی | debts، installments، budgets، payment_methods، cheques، splits، currencies، assets |
| برنامه‌ریزی | recurring، savings_goals، appointments، **notes**، subscriptions، challenges |
| سیستم | activity_logs، telegram_users، trash، prefs |

**نکات ساختاری مهم:**
- `Tx.tag` — تگ اختیاری از نوع `TX_TAGS` (چهار تگ رفتاری)
- `AppState.rev` — شمارندهٔ نسخه؛ با هر `mutate()` یکی بالا می‌رود و مبنای سینک «آخرین نوشتنِ برنده» است
- `prefs.accent`، `prefs.notifyEnabled`، `prefs.syncUrl/syncKey/syncId`
- مهاجرت خودکار: داده‌های قدیمی که `notes` ندارند هنگام بارگذاری ترمیم می‌شوند

**قانون طلایی:** همهٔ تغییرات فقط از `mutate()` عبور می‌کنند (ذخیرهٔ محلی + افزایش rev + لاگ فعالیت) و حذف‌ها فقط با `trashItem()` (سطل ۳۰ ثانیه‌ای).

---

## ۷. معماری همگام‌سازی و چنددستگاهی

```
┌──────────────┐   ذخیره/خواندن    ┌───────────────────┐
│  مرورگر شما  │ ◄──────────────► │ localStorage       │
│ (هر دستگاه)  │                   │ fp_data_<کاربر>    │
└──────┬───────┘                   └───────────────────┘
       │  ۳ ثانیه دیباونس (ارسال) / ۹۰ ثانیه پولینگ (دریافت) / هنگام شروع (سازش‌گیری)
       ▼
┌────────────────────────────────────────────┐
│ Supabase — جدول financepro_state           │
│ کلید: fp-user-<نام‌کاربری>  (نه تصادفی!)    │
│ ستون data + updated_at                     │
└────────────────────────────────────────────┘
```

**چرا دیپلوی جدید داده را پاک نمی‌کند؟** چون داده روی Vercel نیست — در مرورگر و Supabase است.

**سازش‌گیری هنگام شروع (Reconcile):**
1. اگر ابر جدیدتر است (`rev` بالاتر) → دادهٔ ابر بازیابی می‌شود (دستگاه جدید دادهٔ اصلی را می‌گیرد، نه اینکه با دادهٔ خالی رویش بنویسد)
2. اگر ابر خالی یا قدیمی‌تر است → دادهٔ محلی فرستاده می‌شود
3. اگر خطای شبکه/کلید است → هیچ‌چیز فرستاده نمی‌شود تا دادهٔ ابر اشتباهی پاک نشود

**کلیدهای مرورگری:** `fp_data_<id>` (داده)، `fp_session` (نشست)، `fp_accent` (تم)، `fp_cloud` (اتصال مشترک)، `fp_smart` (یادآوری تشخیص هوشمند)، `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` (متغیرهای محیطی با اولویت پایین‌تر از تنظیمات دستی).

---

## ۸. PWA و نصب روی گوشی

- `public/manifest.webmanifest` + `public/icon.svg` + `public/sw.js`
- Service Worker: کش آفلاین (اول شبکه، افتادن روی کش) + کلیک روی اعلان = باز شدن برنامه
- دکمهٔ «نصب اپ» در نوار بالای برنامه (با گرفتن رویداد `beforeinstallprompt`)
- اعلان‌های قرارها از کانال Service Worker رد می‌شوند تا در پس‌زمینه هم برسند

---

## ۹. دستیار صوتی

**معماری نهایی (پس از چهار بازنویسی برای رفع تکرار کلمات — به‌ویژه باگ «برفی» نسخهٔ PWA/اندروید):**

- **تحویل افزایشی بر اساس `resultIndex` (مهم‌ترین اصلاح):** هر جملهٔ نهایی (isFinal) دقیقاً **یک‌بار** و بر اساس ایندکسش تحویل می‌شود و در `sessionFinals` جمع می‌شود؛ `committedUpTo` آخرین ایندکس تحویل‌شده را نگه می‌دارد و تحویلِ دوبارهٔ همان ایندکس نادیده گرفته می‌شود. بازخوانیِ «از ایندکس ۰» آرایهٔ نتایج — که عامل اصلی باگ برفی بود — حذف شده است.
- **توکن نشست (`sessionId`):** هر نشست شمارهٔ یکتا می‌گیرد؛ نشست‌های قدیمی یا هم‌پوشان (که مثلاً از شلیک چندبارهٔ `onend` به‌وجود می‌آیند) باطل می‌شوند و هیچ‌وقت دو نمونهٔ تشخیص همزمان متن تحویل نمی‌دهند.
- **هر نشست = نمونهٔ تازهٔ `SpeechRecognition`** و قبل از شروع، نمونهٔ قبلی `abort()` می‌شود — هیچ نمونهٔ مشترک یا `start()` دوباره روی نمونهٔ قدیم وجود ندارد.
- **گارد شروع دوتایی (`activeRef`) + `Field` از `div` (نه `label`):** کلیک‌های دوبله نشست دوم نمی‌سازند؛ دکمهٔ داخل `<label>` در کرومِ PWA کلیک را دوبار شلیک می‌کرد که حذف شده است.
- `continuous: true` + `interimResults: true` — پیش‌نمایش‌های موقت هرگز تحویل نمی‌شوند، فقط جمله‌های نهایی.
- **الحاق هوشمند `appendSmart` (لایهٔ دفاعی نهایی):** مبنای الحاق، «محتوای واقعیِ لحظه‌ای فیلد» (`fieldRef`) است؛ اگر جمله‌ای از قبل در انتهای فیلد باشد دوباره اضافه نمی‌شود — حتی اگر موتور یک جمله را چند بار بفرستد، فقط یک بار ثبت می‌شود.
- **حالت افزودنی:** متن فیلد هنگام شروع ضبط snapshot می‌شود و گفته‌های جدید به انتهای آن اضافه می‌شوند — **متن قبلی هرگز پاک نمی‌شود**.
- پیش‌نمایش زندهٔ «در حال گوش دادن…» + نشانگر قرمز تپنده؛ دکمه بین «دیکتهٔ صوتی» و «توقف ضبط» عوض می‌شود.
- پیام‌های خطای مشخص: رد دسترسی میکروفون / خطای شبکه با اشاره به PWA / میکروفون پیدا نشد.
- در سه جا استفاده می‌شود: توضیحات تراکنش، متن یادداشت، عنوان قرار (همه با `baseText`).

---

## ۱۰. تشخیص هوشمند و تگ‌های تراکنش

**تشخیص هوشمند** (`detectSmart` در data.tsx): با روشن‌بودن کلید، هنگام تایپ توضیح اجرا می‌شود:
- مبلغ با واحدها: «۵۰ هزار» ← ۵۰٬۰۰۰ | «۲ میلیون» ← ۲٬۰۰۰٬۰۰۰ | «۱٫۵ میلیارد»
- دسته از کلمات کلیدی (اسنپ ← رفت‌وآمد، داروخانه ← سلامت، اجاره ← خانه و…)
- حساب بانکی از نام حساب‌های کاربر (کارت ملت ← حساب ملت)
- نوع درآمد/هزینه از کلمات (واریز/حقوق ← درآمد)
- چیپ‌های «چه چیزهایی حدس زده شد» نمایش داده می‌شود

**تگ‌های تراکنش** — برای تحلیل رفتار خرج:
| تگ | رنگ | معنی |
|---|---|---|
| ضروری | قرمز | خرجی که چاره‌ای جز پرداختش نبود |
| تفریحی | طلایی | برای خوش‌گذرانی |
| میشد بعدا هم خرید | آبی | عجله‌ای نداشت |
| معمولی و قیمتش کم بود خریدم | سبز | ارزان و نیاز معمولی |

---

## ۱۱. تم‌ها و ظاهر

- متغیرهای `--fp-*` در `index.css` — تم روشن/تیره فقط با جابه‌جایی متغیرها
- **۶ تم رنگی ترکیبی** (هرکدام رنگ تأکیدی + رنگ موفقیت را با هم عوض می‌کنند):

| تم | تأکیدی | موفقیت |
|---|---|---|
| زمرد و طلا (پیش‌فرض) | طلایی | زمردی |
| زمردی دارک | طلایی | سبز روشن |
| زعفرانی و فیروزه | نارنجی | فیروزه‌ای |
| یاقوتی و صورتی | قرمز-صورتی | صورتی |
| آبی سلطنتی و آسمان | آبی | آسمانی |
| مسی و زیتونی | مسی | زیتونی |

- انتخاب از: سواچ‌های تنظیمات ← ظاهر برنامه، یا دکمهٔ پالت در نوار بالا (چرخشی)
- `prefers-reduced-motion` رعایت شده — همهٔ انیمیشن‌ها fallback دارند

---

## ۱۲. درس‌های فنی و نکات مهم توسعه

برای توسعه‌دهندهٔ بعدی — این‌ها را **هرگز** نشکنید:

1. **تاریخ شمسی:** هرگز `new Date()` مستقیم برای شمسی نه — همیشه توابع utils (`jalaliToday`، `jalaliToISO`، `isoToJalali`، `periodRange`…)
2. **اعداد:** برای نمایش `faNum()`/`faMoney()` — نه `toLocaleString` خالی
3. **Store مرکزی:** همهٔ تغییرات از `mutate()` — نه دستکاری مستقیم state (سینک و ذخیره از دست می‌رود)
4. **حذف‌ها:** فقط `trashItem()` — حذف مستقیم یعنی از دست رفتن بازگشت ۳۰ ثانیه‌ای
5. **شناسهٔ سینک:** `fp-user-<نام‌کاربری>` — اگر تصادفی شود، دستگاه‌ها دیگر همدیگر را نمی‌بینند (باگ بزرگی بود که حل شد)
6. **شمارندهٔ rev:** مبنای سازش‌گیری است؛ دستکاری دستی‌اش باعث پاک‌شدن دادهٔ دستگاه اصلی می‌شود
7. **SpeechRecognition:** هر نشست = نمونهٔ **تازه** (`new SR()`)؛ `start()` دوباره روی نمونهٔ قدیمی ممنوع (کروم نتایج قبلی را نگه می‌دارد و متن دوبار می‌شود)
8. **دادهٔ نمونه:** برنامه خالی شروع می‌شود؛ نمونه فقط با دکمهٔ صریح در مدیریت
9. **کلیدهای ناشناس در Supabase:** `financepro_state` و `fp_users` را حذف نکنید — دادهٔ همهٔ کاربران آنجاست
10. **postcss.config.mjs خالی ریشه را حذف نکنید** — جلوی خوانده‌شدن تنظیمات مزاحمِ پوشه‌های بالاتر را می‌گیرد
11. **دکمهٔ تعاملی داخل `<label>` ممنوع** — در کروم/PWA کلیک دوبار شلیک می‌شود؛ `Field` باید `div` بماند
12. **گارد همگام برای شروع‌های حساس** (`activeRef` در MicButton) — state ری‌اکت برای جلوگیری از دوبار اجرا کافی نیست
13. **ورودی‌های تکرارشوندهٔ موتور تشخیص را idempotent کنید** — موتور PWA/اندروید بعد از مکث جملهٔ قبلی را دوباره می‌فرستد؛ همیشه بر اساس «محتوای واقعی لحظه‌ای فیلد» الحاق کنید و تکرار را حذف کنید (الگوی `appendSmart`)

---

## ۱۳. کارهای باقی‌مانده و ایده‌های آینده

### از سند تحویل اصلی
- [ ] محدود کردن RLS سوپابیس برای تولید (policy باز است)
- [ ] سینک خودکار دوطرفهٔ کامل (الان «آخرین نوشتن برنده» با rev است)
- [ ] فعال‌سازی ربات تلگرام به‌صورت Serverless روی Vercel (جدول‌ها و دستورات آماده‌اند؛ نیاز به `BOT_TOKEN` از BotFather)
- [ ] اپ موبایل بومی با Capacitor
- [ ] پشتیبان‌گیری خودکار روزانه در تلگرام

### ایده‌های جدید
- [x] تحلیل خودکار تگ‌ها در داشبورد — انجام شد (v1.5)
- [x] اتصال گزارش هوشمند به API یک مدل زبانی — انجام شد (v1.5)
- [x] جست‌وجوی سراسری (تراکنش + یادداشت + قرار + دسته) — انجام شد (v1.5)
- [x] دسته‌بندی یادداشت‌ها و سنجاق‌کردن — انجام شد (v1.5)
- [x] خروجی اکسل: ستون تگ + برگهٔ برچسب‌ها + برگهٔ یادداشت‌ها — انجام شد (v1.5)
- [ ] ویجت «این روز در سال‌های قبل» — مقایسهٔ خرج امروز با همین روز در سال‌های گذشته
- [x] یادآوری هوشمند تگ‌گذاری — بعد از ثبت تراکنش بدون تگ، پیشنهاد برچسب بر اساس دسته (v1.7)
- [x] پیش‌بینی کسری بودجه — هشدار قبل از رسیدن به سقف بودجهٔ هر دسته (v1.7)
- [x] مقایسهٔ ماهانه — نمودار کنار هم این ماه و ماه قبل برای هر دسته (v1.7)
- [ ] حالت مرور امن — محو کردن خودکار مبالغ هنگام اسکرین‌شات/اشتراک‌گذاری
- [ ] **خواندن خودکار پیامک بانکی — اپ همراه اندروید** (مرورگر/PWA حق دسترسی به SMS ندارد؛ نیاز به یک اپ کوچک Kotlin/React-Native با سرویس SmsListener دارد که پیام را لوکال پارس و به جدول `financepro_state` در Supabase هل بدهد — مدل اپ‌هایی مثل «حسابدار»)
- [ ] **ثبت با فوروارد پیامک به ربات تلگرام** — پیام بانک را برای ربات فوروارد کنی؛ همان `parseBankSMS` سمت سرور اجرا و تراکنش ثبت می‌شود (بدون نیاز به هیچ اجازه‌ای روی گوشی)
- [x] خروجی PDF گزارش هوشمند + تحلیل هوش مصنوعی به‌صورت یک سند قابل‌ارسال (v1.7)
- [ ] دسته‌بندی و رنگ‌بندی قرارها + تکرار قرارها (هفتگی/ماهانه)
- [ ] همگام‌سازی یادداشت‌ها و تگ‌ها با ربات تلگرام (ثبت یادداشت از تلگرام)
- [x] جست‌وجوی صوتی — گفتن عبارت برای جست‌وجو به‌جای تایپ (v1.7)
- [x] حالت «روزانه» — یک نمای خلاصهٔ امروز: خرج امروز، قرارهای امروز، یادآوری‌ها (v1.7 — صفحهٔ مستقل «روزانه» در منو؛ طبق خواستهٔ کاربر به داشبورد اضافه نشد)

---

## ۱۴. لاگ تغییرات (پس از انتشار نسخهٔ ۱)

> 📌 **قرارداد:** از این پس **هر تغییری** که در پروژه انجام می‌شود باید در این جدول ثبت شود — با شماره نسخه، شرح، و فایل‌های لمس‌شده.

| نسخه | تغییر | فایل‌ها |
|---|---|---|
| v1.1 | **رفع تکرار کلمات دستیار صوتی در نسخهٔ PWA/اندروید:** ۱) هر نشست تشخیص حالا نمونهٔ تازهٔ `SpeechRecognition` می‌سازد (قبلاً `rec.start()` روی نمونهٔ قدیمی، نتایج نشست قبل را دوباره به متن اضافه می‌کرد) ۲) گارد همگام `activeRef` جلوی شروع دوتایی نشست را می‌گیرد ۳) کامپوننت `Field` از `<label>` به `<div>` تبدیل شد — دکمهٔ داخل label در کرومِ PWA کلیک را دوبار شلیک می‌کرد ۴) پیام خطای شبکه به نسخهٔ نصب‌شده اشاره می‌کند | `src/ui.tsx` |
| v1.3 | **پنج اصلاح ساختاری (بازبینی امنیتی/منطقی):** ۱) ثبت Service Worker تأیید شد (قبلاً در `main.tsx` با نگهبان PROD وجود داشت — در نسخهٔ Vercel فعال است؛ نگهبان PROD عمداً حفظ شد تا کشِ کهنه در محیط توسعه ایجاد نکند) ۲) **رفع باگ منطقهٔ زمانی:** تابع `localISODate()` اضافه شد و `todayISO()` و همهٔ جاهایی که از `toISOString()` برای «امروز» استفاده می‌کردند (اسپارک‌لاین داشبورد، یادآوری و زنگِ قرارها، تاریخ میلادی گزارش هوشمند، `relTime`) به اجزای محلی تبدیل شدند — ایران UTC+3:30 است و بین ۰۰:۰۰ تا ۰۳:۳۰ بامداد، `toISOString` روز قبل را برمی‌گرداند و فیلتر «امروز» و یادآوری‌ها یک روز عقب می‌افتادند ۳) **امنیت:** `pushToCloud` دیگر کل `prefs` را به ابر نمی‌فرستد؛ فقط `syncId` ارسال می‌شود تا پین/کلید سینک/توکن‌ها در جدول Supabase ذخیره نشوند ۴) **جلوگیری از گم شدن تغییرات در سینک:** تابع `mergePulledState()` جایگزین `Object.assign` ساده شد — هنگام دریافت از ابر، تراکنش‌های محلیِ سینک‌نشده (با dedupe بر اساس id) حفظ می‌شوند و تعدادشان در توست اعلام می‌شود ۵) **تراکنش دوره‌ای در ماه‌های کوتاه:** روز مؤثر (`Math.min(dayOfMonth, طول ماه)`) قبل از مقایسه محاسبه می‌شود تا `dayOfMonth=31` در ماه‌های ۲۹/۳۰ روزه هم اجرا شود؛ برچسب فیلد هم به «۱-۳۱ — آخر ماه در ماه‌های کوتاه» تغییر کرد | `src/main.tsx`، `src/lib/utils.ts`، `src/lib/cloud.ts`، `src/lib/data.tsx`، `src/App.tsx`، `src/pages.tsx`، `src/pages2.tsx` |
| v1.2 | **رفع قطعی باگ «برفی» دستیار صوتی** (تکرار روزافزون کلمه‌ها: «می‌کند می‌کند می‌کند…» در PWA/اندروید): ریشه این بود که `onresult` هر بار آرایهٔ نتایج را از ایندکس ۰ بازخوانی می‌کرد و موتورِ اندروید نتیجه‌های قبلی را دوباره داخل آرایه می‌فرستاد → متن روزافزون تکرار می‌شد. اصلاح: ۱) **تحویل افزایشی بر اساس `resultIndex`** — هر جملهٔ نهایی دقیقاً یک‌بار و بر اساس ایندکسش تحویل می‌شود (`committedUpTo`)، بازخوانی از صفر حذف شد ۲) **توکن نشست (`sessionId`)** — هر نشست شمارهٔ یکتا می‌گیرد و نشست‌های قدیمی/هم‌پوشان باطل می‌شوند، پس هیچ‌وقت دو نمونهٔ تشخیص همزمان متن نمی‌دهند ۳) قبل از هر نشست، نمونهٔ قبلی `abort()` می‌شود. `appendSmart` به‌عنوان لایهٔ دفاعی نهایی باقی ماند | `src/ui.tsx` |
| v1.2 | **رفع قطعی تکرار جملهٔ دستیار صوتی در PWA:** ریشهٔ واقعی این بود که موتور اندروید بعد از هر مکث، جملهٔ قبلی را دوباره در نشست بعدی می‌فرستد و کد قدیم آن را دوباره به متن اضافه می‌کرد. حالا تابع **الحاق هوشمند `appendSmart`** اضافه شد که بر اساس «محتوای واقعیِ لحظه‌ای فیلد» (`fieldRef`) کار می‌کند و اگر جمله‌ای از قبل در انتهای فیلد باشد دوباره اضافه‌اش نمی‌کند — کاملاً idempotent. حتی اگر موتور یک جمله را چند بار بفرستد، فقط یک بار ثبت می‌شود. | `src/ui.tsx` |
| v1.4 | **مدیریت برچسب‌های (تگ‌های) تراکنش:** تگ‌ها از یک ثابتِ کد (`TX_TAGS`) به دادهٔ قابل‌ویرایش در state (`tags: TagDef[]`) تبدیل شدند تا کاربر بتواند تگ دلخواه بسازد و تگ‌های موجود را ویرایش یا حذف کند. ۱) در `data.tsx`: نوع `TagDef` و `DEFAULT_TAGS` (چهار تگ پیش‌فرض با `builtin: true`) تعریف شد، `tags` به `AppState` و `TableName` اضافه شد (تا حذف/بازگردانی ۳۰ ثانیه‌ای کار کند)، مهاجرت برای state‌های قدیمی، و گزینش‌گرهای `getTags`/`tagById`. نوع `Tx.tag` به `ID` تغییر کرد ۲) تب جدید **«برچسب‌ها»** در صفحهٔ مدیریت با فیلدهای نام، توضیح و رنگ (swatch قابل‌کلیک)، نمایش تعداد تراکنش‌های هر تگ و نشان «پیش‌فرض» ۳) فرم ثبت تراکنش، فیلتر تراکنش‌ها و گزارش هوشمند همه از `getTags(state)` می‌خوانند. تگ‌ها مثل دسته‌ها جزء ساختارند و در «پاک‌سازی داده‌ها» حذف نمی‌شوند | `src/lib/data.tsx`، `src/pages.tsx`، `src/pages2.tsx` |
| v1.5 | **چهار قابلیت تحلیلی و آپدیت اکسل:** ۱) **کارت «تحلیل رفتار خرج این ماه» در داشبورد** — توزیع تگ‌ها با نوار و ٪ هر تگ، به‌همراه جملات تحلیلی خودکار (بیشترین تگ، پتانسیل پس‌انداز از تگ‌های غیرضروری، تعداد تراکنش‌های بدون برچسب) ۲) **اتصال گزارش هوشمند به API مدل زبانی** — در مودال گزارش هوشمند یک بخش تنظیمات (آدرس API پیش‌فرض OpenRouter، کلید، مدل) اضافه شد که در prefs ذخیره می‌شود؛ دکمهٔ «تحلیل با هوش مصنوعی» پاسخ را به‌صورت زنده (streaming) نمایش می‌دهد. کلید API مثل syncKey هرگز به ابر فرستاده نمی‌شود ۳) **جست‌وجوی سراسری از نوار بالا** — جستجوی همزمان تراکنش، یادداشت، قرار و دسته با گروه‌بندی نتایج و پرش به صفحهٔ مربوطه (جایگزین جستجوی فقط-تراکنش شد) ۴) **دسته‌بندی و سنجاق‌کردن یادداشت‌ها** — فیلد دسته (با پیشنهاد از دسته‌های قبلی)، فیلتر دسته، دکمهٔ سنجاق/برداشتن، و نمایش یادداشت‌های سنجاق‌شده اول از همه ۵) **آپدیت خروجی اکسل** — ستون «تگ» به برگهٔ تراکنش‌ها، برگهٔ جدید «برچسب‌ها» (نام، توضیح، تعداد، جمع، سهم از هزینه) و برگهٔ جدید «یادداشت‌ها» (عنوان، دسته، تاریخ، سنجاق، متن) | `src/pages.tsx`، `src/pages2.tsx`، `src/App.tsx`، `src/lib/data.tsx`، `src/excel.ts` |
| v1.6 | **پشتیبانی از کلیدهای جدید گوگل (`AQ.`) در اتصال هوش مصنوعی:** کلیدهای «شخصی» جدید Google AI Studio که با `AQ.` شروع می‌شوند فقط با فرمت بومی گوگل کار می‌کنند (هدر `X-goog-api-key` + بدنهٔ `contents/parts` + endpoint `streamGenerateContent?alt=sse`) و با فرمت OpenAI/Bearer که قبلاً استفاده می‌شد خطا می‌دادند. حالا `analyzeWithAI` پروتکل را از روی آدرس تشخیص می‌دهد: آدرس‌های `generativelanguage.googleapis.com` (چه مسیر `/openai/` چه پایه) → فرمت بومی گوگل؛ بقیه → فرمت OpenAI. در نتیجه هر دو نوع کلید (`AIza` و `AQ.`) و هر سه ارائه‌دهنده (Gemini رایگان، OpenRouter، OpenAI) کار می‌کنند. پیش‌فرض ارائه‌دهنده روی آدرس پایهٔ گوگل و مدل `gemini-flash-latest` تنظیم شد؛ پارسر stream هم فرمت پاسخ گوگل (`candidates[0].content.parts`) و OpenAI (`choices[0].delta`) را هر دو می‌فهمد و پیام‌های خطای دقیق‌تری (HTTP + بدنهٔ پاسخ، دلیل مسدودی گوگل) نشان می‌دهد | `src/pages2.tsx` |
| v1.6 | **پشتیبانی از کلید رایگان Google Gemini + انتخاب‌گر ارائه‌دهنده:** چون فرمت درخواست سایت (OpenAI با `stream: true`) دقیقاً با اندپوینت سازگار-با-OpenAIِ گوگل یکی است، کلید رایگان Gemini هم کار می‌کند. ۱) ثابت `AI_PROVIDERS` با سه پیش‌تنظیم (Gemini/OpenRouter/OpenAI) اضافه شد ۲) در تنظیمات هوش مصنوعی یک انتخاب‌گر «ارائه‌دهنده» آمد که با یک کلیک آدرس و مدل را پر می‌کند ۳) پیش‌فرض روی Gemini گذاشته شد (`gemini-2.0-flash`) تا کاربر فقط کلید رایگانش را از aistudio.google.com وارد کند. کلید همچنان فقط در مرورگر ذخیره و هرگز به ابر فرستاده نمی‌شود | `src/pages2.tsx` |
| v1.7 | **شش قابلیت از نقشهٔ راه (بدون تغییر در داشبورد، طبق خواستهٔ کاربر):** ۱) **یادآوری هوشمند تگ‌گذاری** — بعد از ثبت تراکنشِ هزینهٔ بدون تگ، مودال همان‌جا می‌ماند و برچسب‌ها را پیشنهاد می‌دهد؛ تگِ هم‌خوان با دسته (با کلیدواژه‌های `TAG_SUGGEST_KEYWORDS`) با نشان «پیشنهادی» اول می‌آید؛ دکمهٔ «بدون برچسب — رد شو» برای گذشتن ۲) **پیش‌بینی کسری بودجه** — تابع `budgetCheck` هنگام ثبت/ویرایش هر هزینه، مصرف ماهِ دسته را حساب می‌کند: عبور از سقف → توست قرمز، رسیدن به ٪۸۰ → توست هشدار؛ ردیف‌های بودجه در مدیریت هم سه‌رنگه شدند (سبز/هشدار/قرمز) با برچسب «نزدیک سقف» و «مازاد بر سقف» ۳) **مقایسهٔ ماهانه** — کارت جدید در صفحهٔ گزارش‌ها: نمودار میله‌ای دوتایی (این ماه در برابر ماه قبل) برای ۷ دستهٔ برتر + چیپ تغییر درصد هر دسته (▲ افزایش قرمز، ▼ کاهش سبز، «جدید») ۴) **خروجی PDF گزارش هوشمند** — دکمهٔ «خروجی PDF» در مودال گزارش هوشمند، سند چاپی جداگانه‌ای (RTL با فونت لاله‌زار/وزیرمتن) شامل گزارش + تحلیل هوش مصنوعی باز می‌کند که از راه Print → Save as PDF قابل‌ارسال است ۵) **جست‌وجوی صوتی** — دکمهٔ میکروفون در نوار جست‌وجوی سراسری؛ عبارت گفته‌شده (fa-IR) جایگزین متن ورودی می‌شود و نتایج همان‌جا ظاهر می‌شوند؛ گارد نشست تکی و پیام‌های خطای مشخص دارد ۶) **حالت روزانه** — صفحهٔ جدید «روزانه» در منو (نه داشبورد): آمار زندهٔ امروز (خرج/درآمد/قرارهای باقی‌مانده) با ساعت زنده، برنامهٔ امروز با تیک انجام‌شدن، یادآوری‌های هفتهٔ پیشِ رو (بدهی‌های سررسید‌نزدیک، بودجه‌های ٪۸۰+، تمدید اشتراک‌های ۷ روز آینده) و تراکنش‌های امروز | `src/pages.tsx`، `src/pages2.tsx`، `src/App.tsx` |
| v1.8 | **آیکون برای دسته‌ها (به‌جای دایرهٔ رنگی ساده):** ۱) فیلد `icon?: string` به `Category` اضافه شد + نقشهٔ `CATEGORY_ICON_BY_NAME` برای آیکون پیش‌فرض هر دسته ۲) در `ui.tsx` کتابخانهٔ ۲۸ آیکون دسته (`CATEGORY_ICONS` + برچسب فارسی `CATEGORY_ICON_LABELS`) و دو کامپوننت `CatGlyph` (آیکون داخل کاشی رنگی) و `CatIconInline` (آیکون سادهٔ درون‌خطی) تعریف شد ۳) دایره‌های رنگی در گزارش دسته‌ها، نتایج جست‌وجوی سراسری، تراکنش‌های صفحهٔ روزانه و ردیف‌های مدیریت با CatGlyph جایگزین شدند؛ چیپ نام دسته در تراکنش‌ها هم آیکون کوچک گرفت ۴) فرم ثبت/ویرایش دسته در مدیریت یک **انتخاب‌گر آیکون** (۲۸ آیکون قابل‌کلیک با tooltip فارسی) دارد — نوع فیلد جدید `icon` به فرم‌ساز عمومی اضافه شد و پیش‌فرضش `wallet` است ۵) مهاجرت: دسته‌های قدیمی بدون آیکون هنگام بارگذاری، آیکون پیش‌فرضِ نامشان را می‌گیرند | `src/lib/data.tsx`، `src/ui.tsx`، `src/pages.tsx`، `src/pages2.tsx`، `src/App.tsx` |
| v1.9 | **۱۰ آیکون جدید برای دسته‌ها:** میوه (apple)، دانهٔ قهوه (coffee — برچسبش از «کافه» به «دانهٔ قهوه» تغییر کرد)، کافه (cup-soda — چون MugHot در نسخهٔ lucide موجود نیست)، نان و شیرینی (croissant)، پراپ تریدینگ (candlestick = CandlestickChart)، قبوض (receipt)، گوشت و پروتئین (beef)، اینترنت (wifi)، شخص مرد (male) و شخص زن (female) — این دو چون در lucide نیستند به‌صورت SVG دستی (نمادهای مریخ/ونوس) کشیده و به‌عنوان `CatIcon` تعریف شدند؛ وام و تسهیلات (landmark). نوع `CATEGORY_ICONS` از `Record<string, LucideIcon>` به `Record<string, CatIcon>` تغییر کرد تا هم آیکون‌های lucide و هم SVGهای دستی را بپذیرد. نقشهٔ `CATEGORY_ICON_BY_NAME` هم با نام‌های جدید (میوه، قهوه، کافه، نان، پراپ تریدینگ، قبوض، گوشت، اینترنت، مرد، زن، وام و…) به‌روز شد تا دسته‌های ساخته‌شده با این نام‌ها خودکار آیکون درست بگیرند | `src/ui.tsx`، `src/lib/data.tsx` |
| v1.11 | **پارسر پیام بانکی مطابق فرمت واقعی بانک‌های ایران (بانک ملی و…):** فرمت واقعی مثل `انتقال:4,509,000-` + `حساب:83008` + `مانده:220,654,858` + `0531-20:40` پشتیبانی می‌شود. اصلاح‌ها: ۱) علامت منفی/مثبتِ چسبیده به مبلغ (قبل یا بعد از عدد) نوع تراکنش را تعیین می‌کند — در غیر این صورت کلیدواژه (واریز/دریافت ← درآمد؛ خرید/برداشت/پرداخت/انتقال ← هزینه) ۲) وقتی واحد پول در پیام نیست، «ریال» (عرف پیامک‌های بانکی ایران) فرض و **خودکار به تومان** تبدیل می‌شود + یادداشت تحلیلی برای بازبینی کاربر ۳) تاریخ `MMDD-HH:MM` بدون سال (فرمت ملی) با حدس سال از تاریخ امروز (ماه بزرگ‌تر از ماه جاری ← سال قبل) و اعتبارسنجی طول ماه شمسی ۴) شمارهٔ `حساب:NNNNN` استخراج و برای تطبیق حساب استفاده می‌شود ۵) تطبیق سه‌مرحله‌ای حساب: چهار رقم کارت ← شمارهٔ حساب ← نام بانکِ داخل پیام با کلمات معنادار نام حساب (مثل «ملی») ۶) نرمال‌سازی کاف/یای عربی (`بانك` ← `بانک`) و نیم‌فاصله‌ها ۷) آرایهٔ `notes` برای یادداشت‌های تحلیلی و سه نمونهٔ واقعی (ملی/خرید ریالی/واریز حقوق ملت) با چیپ «نمونهٔ واقعی» که با یک کلیک پیام را پر و تحلیل می‌کند | `src/lib/sms.ts`، `src/pages.tsx` |
| v1.10 | **ثبت تراکنش از پیام بانکی (پارسر SMS):** مرورگر به‌دلایل امنیتی نمی‌تواند پیامک‌ها را بخواند، بنابراین به‌جای آن ماژول جدید `src/lib/sms.ts` (`parseBankSMS` + `matchAccountByCard`) ساخته شد که متن پیام بانکیِ چسبانده‌شده را **کاملاً روی دستگاه کاربر** تحلیل می‌کند: ۱) استخراج مبلغ با تشخیص واحد — **تبدیل خودکار ریال به تومان** (بانک‌ها ریال می‌فرستند)، پشتیبانی از «میلیون ریال/هزار ریال»، ارقام و جداکننده‌های فارسی ۲) تاریخ **شمسی داخل پیام** (مثل 1403/05/12) با اعتبارسنجی و تبدیل به ISO میلادی با `jalaliToISO` ۳) ساعت ۴) چهار رقم آخر کارت (الگوهای «کارت *4321»، «انتهای 4321»، شمارهٔ کامل ماسک‌شده) و **انتخاب خودکار حساب** از روی رقم پایان کارت ۵) طرف مقابل («خرید از…/پرداخت به…») که هم در توضیحات می‌نشیند و هم برای **حدس دسته** با `detectSmart` استفاده می‌شود ۶) ماندهٔ حساب. در فرم تراکنش (`TxModal`) پنل تاشوی آبی «ثبت از پیام بانکی» اضافه شد: پیست پیام ← «تحلیل پیام» ← فرم (نوع، مبلغ، تاریخ، حساب، توضیح، دسته) خودکار پر می‌شود و کارت نتیجه با سطح اطمینان (بالا/چک‌کردن واحد) نمایش داده می‌شود. پیام هیچ‌جا ارسال نمی‌شود | `src/lib/sms.ts`، `src/pages.tsx` |

---

*این سند همراه پروژه نگه داده می‌شود و با **هر تغییر** باید به‌روز شود (بخش ۱۴). — فایننس‌پرو، ساخته‌شده با ❤️ برای کاربران فارسی‌زبان*
