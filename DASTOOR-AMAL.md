# 📦 سند دستور عمل پروژهٔ فایننس‌پرو (FinancePro)

> دفترکل دیجیتال شخصی برای کاربران فارسی‌زبان — PWA با تقویم شمسی، اعداد فارسی و ربات تلگرام
>
> این سند **تاریخچهٔ کامل پروژه** از نسخهٔ ۱ تا امروز را ثبت می‌کند و مرجع دائمی توسعه‌دهندهٔ بعدی است.
> **قرارداد:** هر تغییری که در پروژه انجام می‌شود باید در [بخش ۱۴ (لاگ تغییرات)](#۱۴-لاگ-تغییرات) ثبت شود.

---

## فهرست مطالب

1. [معرفی](#۱-معرفی)
2. [پشتهٔ فناوری](#۲-پشتهٔ-فناوری)
3. [اجرا و استقرار](#۳-اجرا-و-استقرار)
4. [ساختار فعلی فایل‌ها](#۴-ساختار-فعلی-فایل‌ها)
5. [فهرست کامل قابلیت‌ها](#۵-فهرست-کامل-قابلیت‌ها)
6. [مدل داده](#۶-مدل-داده)
7. [معماری سینک](#۷-معماری-سینک)
8. [PWA و اپ اندروید](#۸-pwa-و-اپ-اندروید)
9. [دستیار صوتی](#۹-دستیار-صوتی)
10. [تشخیص هوشمند، پارسر پیام بانکی و تگ‌ها](#۱۰-تشخیص-هوشمند-پارسر-پیام-بانکی-و-تگ‌ها)
11. [تم‌های رنگی](#۱۱-تم‌های-رنگی)
12. [درس‌های فنی و نکات مهم توسعه](#۱۲-درس‌های-فنی-و-نکات-مهم-توسعه)
13. [کارهای باقی‌مانده و ایده‌های آینده](#۱۳-کارهای-باقی‌مانده-و-ایده‌های-آینده)
14. [لاگ تغییرات](#۱۴-لاگ-تغییرات)

---

## ۱. معرفی

فایننس‌پرو یک وب‌اپلیکیشن مدیریت مالی شخصی است که برای کاربران ایرانی طراحی شده:

- **کاملاً راست‌به‌چپ (RTL)** و فارسی
- **تقویم شمسی (جلالی)** در همه‌جا — ذخیره به میلادی، نمایش به شمسی
- **اعداد فارسی** با جداکنندهٔ هزارگان
- **چندکاربره** — هر کاربر حساب و دادهٔ جداگانه دارد + حالت مهمان
- **PWA** — قابل نصب روی موبایل و دسکتاپ، کارکرد آفلاین
- **سینک ابری Supabase** — داده‌ها با هر دیپلوی Vercel امن می‌مانند
- **اپ اندروید با Capacitor** — پیکربندی آماده (راهنما: `CAPACITOR-GUIDE.md`)
- **گزارش هوشمند + اتصال به هوش مصنوعی** — تحلیل رفتار مالی

### اصول حاکم

1. **میلیادی ذخیره، شمسی نمایش** — همهٔ تاریخ‌ها ISO ذخیره می‌شوند؛ تبدیل شمسی فقط در لایهٔ نمایش
2. **Store مرکزی** — هر تغییر از `mutate()` عبور می‌کند تا ذخیرهٔ محلی + سینک ابری خودکار انجام شود
3. **حذف امن** — هیچ‌چیز فوراً پاک نمی‌شود؛ سطل ۳۰ ثانیه‌ای با نوار بازگردانی
4. **دادهٔ کاربر مقدس است** — بازیابی، مهاجرت و سینک هیچ‌وقت داده را گم نمی‌کنند

---

## ۲. پشتهٔ فناوری

| حوزه | فناوری |
|---|---|
| فریم‌ورک | React 18 + Vite 6 |
| زبان | TypeScript 5.7 |
| استایل | Tailwind CSS v4 با CSS Variables (`--fp-*`) برای تم |
| فونت | Lalezar (تیتر) + Vazirmatn (متن) — Google Fonts |
| تاریخ شمسی | jalaali-js |
| نمودار | Recharts + Sparkline دست‌ساز SVG |
| آیکن | Lucide React + آیکن‌های SVG دست‌ساز (مرد/زن) |
| اکسل | ExcelJS — خروجی ۸ برگی حرفه‌ای |
| بک‌اند/دیتابیس | Supabase (PostgreSQL + REST) — جداول `financepro_state` و `fp_users` |
| میزبانی سایت | Vercel (دیپلوی خودکار از GitHub) |
| اپ بومی | Capacitor 8 (پیکربندی آماده — APK نهایی با Android Studio) |
| ربات تلگرام | طراحی‌شده در سند اصلی — زیرساخت Webhook آماده |

---

## ۳. اجرا و استقرار

### اجرای لوکال

```bash
npm install        # فقط بار اول
npm run dev        # → http://localhost:3000
npm run build      # بیلد تولید (خروجی در dist/)
```

⚠️ فایل `postcss.config.mjs` خالی در ریشه را **حذف نکنید** — جلوی خوانده‌شدن تنظیمات مزاحمِ پوشه‌های بالاتر را می‌گیرد (خطای `@tailwindcss/postcss` در ویندوز).

### مراحل استقرار (همان‌طور که با کاربر انجام شد)

| مرحله | کار | وضعیت |
|---|---|---|
| ۱ | ساخت پروژهٔ رایگان Supabase | ✅ |
| ۲ | اجرای SQL ساخت جدول‌ها در SQL Editor | ✅ |
| ۳ | برداشتن آدرس پروژه و کلید anon | ✅ |
| ۴ | دیپلوی روی Vercel از GitHub (ریپازیتوری `tajeddini/FINANCEPRO2`) | ✅ |
| ۵ | اتصال سایت به Supabase از تنظیمات برنامه | ✅ |
| ۶ | ساخت جدول `fp_users` برای ورود چنددستگاهی | ✅ |
| ۷ | (اختیاری) متغیرهای محیطی `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` در Vercel | ⏳ |

### کد SQL جدول‌های Supabase

```sql
-- دفترکل مشترک سایت و ربات (هر کاربر یک ردیف)
create table if not exists public.financepro_state (
  id         text primary key,
  data       text,
  updated_at timestamptz default now()
);
alter table public.financepro_state enable row level security;
create policy "open_anon" on public.financepro_state
  for all using (true) with check (true);

-- کاربران برای ورود چنددستگاهی
create table if not exists public.fp_users (
  username   text primary key,
  data       text,
  updated_at timestamptz default now()
);
alter table public.fp_users enable row level security;
create policy "open_anon" on public.fp_users
  for all using (true) with check (true);
```

### مشکلات دیپلوی که حل شدند (درس‌های Vercel)

| مشکل | دلیل | راه‌حل |
|---|---|---|
| `vite: command not found` | وابستگی‌ها نصب نشده بودند | `Install Command` = `npm install` |
| `ENOENT package.json` | جای Install/Build عوض شده بود | Build = `npm run build`، Install = `npm install` |
| پوشهٔ تودرتو | پروژه داخل پوشه آپلود شده بود | `Root Directory` در تنظیمات Vercel |
| خطای `@tailwindcss/postcss` | فایل postcss مزاحم در پوشهٔ بالاتر | `postcss.config.mjs` خالی در ریشهٔ پروژه |

---

## ۴. ساختار فعلی فایل‌ها

```
financepro/
├── index.html                  ← ورودی HTML (RTL، فونت‌ها، تم اولیه بدون فلاش)
├── postcss.config.mjs          ← خالی — سپر در برابر postcss مزاحم
├── capacitor.config.ts         ← پیکربندی اپ اندروید (Vite نمی‌خواندش)
├── patch-android-mirror.cjs    ← پچ میرور Aliyun برای Gradle در ایران
├── CAPACITOR-GUIDE.md          ← راهنمای کامل ساخت APK
├── DASTOOR-AMAL.md             ← همین سند
├── assets/
│   └── icon.svg                ← آیکون برند اپ (زمردی + نمودار طلایی)
├── public/                     ← فایل‌های PWA
│   ├── manifest.webmanifest    ← نصب‌پذیری (نام فارسی، RTL، رنگ برند)
│   ├── icon.svg
│   └── sw.js                   ← Service Worker (کش آفلاین + اعلان‌ها)
└── src/
    ├── main.tsx                ← بوت‌استرپ + ثبت SW (فقط در PROD)
    ├── App.tsx                 ← پوسته: ورود/ثبت‌نام، ناوبری ۱۰ صفحه، جست‌وجوی سراسری،
    │                              نوار بازگشت ۳۰ ثانیه، زنگ یادآوری، سینک خودکار،
    │                              اتصال ابری صفحهٔ ورود، قفل پین، دکمه‌های تم
    ├── index.css               ← سیستم طراحی کامل (--fp-*) + ۶ تم رنگی + چاپ
    ├── ui.tsx                  ← کتابخانهٔ UI: توست، مودال، تقویم شمسی، MicButton،
    │                              فرم تراکنش مشترک، ۳۸ آیکون دسته، PeriodFilter،
    │                              usePeriod، دکمه‌های ویرایش/حذف پررنگ
    ├── pages.tsx               ← داشبورد (کارت تحلیل تگ‌ها)، تراکنش‌ها (تگ، پیام بانکی،
    │                              پیش‌بینی بودجه)، گزارش دسته‌ها (بازشدن درجا)،
    │                              بدهی‌ها (ویرایش/حذف + تسویه با حساب)
    ├── pages2.tsx              ← قرارها (TimeWheel + زنگ)، یادداشت‌ها (دسته + سنجاق)،
    │                              گزارش‌ها (مقایسهٔ ماهانه + گزارش هوشمند + AI + PDF)،
    │                              روزانه، مدیریت (۱۳ ابزار + برچسب‌ها + دادهٔ نمونه)،
    │                              تنظیمات (تم‌ها، امنیت، سینک، انتقال، حساب)
    ├── widgets.tsx             ← Sparkline، Heatmap، ScoreRing، Forecast، نشان‌ها، سلامت مالی
    ├── excel.ts                ← ExcelJS ۸ برگی + CSV سازگار با اعداد فارسی
    ├── vite-env.d.ts
    └── lib/
        ├── data.tsx            ← مدل داده + Store مرکزی + مهاجرت + تشخیص هوشمند +
        │                          تگ‌ها (DEFAULT_TAGS) + نمونه/پاک‌سازی
        ├── utils.ts            ← شمسی + اعداد فارسی + بازه‌های زمانی (۱۰ فیلتر) +
        │                          تاریخ محلی + زنگ صوتی + اعلان + هوک‌ها
        ├── auth.ts             ← احراز هویت چندکاربره (هش djb2 + بک‌فیل ابری)
        ├── cloud.ts            ← سینک Supabase (امن + ادغام بدون گم‌شدن) + کاربران ابری +
        │                          تنظیمات مشترک + متغیرهای محیطی
        ├── sms.ts              ← پارسر پیام‌های بانکی (ملی، رسالت و…)
        ├── themes.ts           ← ۶ تم رنگی ترکیبی
        └── native.ts           ← تشخیص پلتفرم Capacitor (فاز آماده‌سازی)
```

---

## ۵. فهرست کامل قابلیت‌ها

### صفحه‌ها (۱۰ صفحه)

| صفحه | قابلیت‌ها |
|---|---|
| **داشبورد** | موجودی/درآمد/هزینه با مخفی‌سازی جداگانه، مخفی‌سازی جدا برای موجودی حساب‌ها، ثبت سریع، کارت **«تحلیل رفتار خرج این ماه»** (توزیع تگ‌ها + پتانسیل پس‌انداز با شناسایی «ضروری»)، فعالیت اخیر، چالش، ارز، اشتراک |
| **روزانه** | نمای خلاصهٔ امروز: خرج/درآمد/قرارهای باقی‌مانده با ساعت زنده، برنامهٔ امروز با تیک، یادآوری‌های ۷ روز پیشِ رو (بدهی، بودجهٔ ٪۸۰+، تمدید اشتراک)، تراکنش‌های امروز |
| **تراکنش‌ها** | ۹ فیلتر آماده + **بازهٔ دلخواه** (دو تقویم شمسی)، فیلتر نوع/دسته/**تگ**، جستجو، **ثبت از پیام بانکی** (۳ نمونهٔ واقعی)، ویرایش، حذف با بازگشت، ورود/خروج CSV فارسی، **پیش‌بینی کسری بودجه** (هشدار ٪۸۰ + خطای عبور) |
| **گزارش دسته‌ها** | دونات + نوارهای متحرک با آیکون دسته، کلیک روی دسته → **تراکنش‌هایش در همان صفحه** بر اساس بازهٔ انتخابی |
| **بدهی‌ها** | بدهی/طلب/اقساط، **ویرایش و حذف**، تسویه با **انتخاب حساب و ثبت تراکنش**، ماشین‌حساب وام، QR |
| **قرارها** | تقویم شمسی با نقطهٔ رویداد، **TimeWheel** (ساعت زنده + شبکهٔ ساعت/دقیقه + گزینه‌های سریع)، ICS، **یادآور صوتی** (زنگ + اعلان سیستم، حتی پس‌زمینه با SW) |
| **یادداشت‌ها** | برگه‌های چسبان رنگی، **دسته‌بندی + فیلتر دسته**، **سنجاق**، دیکتهٔ صوتی، ویرایش/حذف ۳۰ ثانیه‌ای |
| **گزارش‌ها** | فیلترهای زمانی کامل، نمودار ۶ ماهه، پیش‌بینی، امتیاز سلامت، نقشهٔ حرارتی، نشان‌ها، **مقایسهٔ ماهانه** (دومیله‌ای + ٪ تغییر)، **گزارش هوشمند** (۱۴ بخش متنی) + **تحلیل زنده با هوش مصنوعی** (Gemini رایگان/OpenRouter/OpenAI) + **خروجی PDF** + **اکسل حرفه‌ای برحسب فیلتر** |
| **مدیریت** | ۱۳ ابزار: حساب، دسته (با **آیکون**)، **برچسب‌ها (ساخت/ویرایش/حذف تگ)**، انتقال، دوره‌ای، هدف، بودجه، روش پرداخت، چک، اشتراک، دارایی، چالش، ارز + **دادهٔ نمونهٔ اختیاری + پاک‌سازی** + **هشدار حذف حسابِ دارای تراکنش** |
| **تنظیمات** | **۶ تم رنگی**، تم روشن/تیره، پین، **اتصال Gemini/OpenRouter/OpenAI**، شناسهٔ سینک + ربات تلگرام، سینک Supabase، **انتقال بین مرورگرها (کد)**، پشتیبان/بازیابی JSON (با حفظ prefs و مهاجرت)، فهرست کاربران دستگاه، حذف حساب |

### قابلیت‌های عرضی

- **ورود چنددستگاهی:** حساب در `fp_users` ثبت می‌شود؛ در دستگاه ناشناس با `pullUser` پیدا و بک‌فیل می‌شود
- **جست‌وجوی سراسری:** تراکنش + یادداشت + قرار + دسته از نوار بالا + **جست‌وجوی صوتی** (فا-IR)
- **حذف با بازگشت:** سطل ۵ تایی، نوار بازگردانی برای **همهٔ موارد معلق** با شمارش معکوس
- **دیکتهٔ صوتی:** در توضیحات تراکنش، متن یادداشت، عنوان قرار و جست‌وجو
- **PWA:** کش آفلاین + اعلان از Service Worker

---

## ۶. مدل داده

### جداول AppState (۲۰ جدول)

| جدول | توضیح | نکته |
|---|---|---|
| `accounts` | حساب‌ها با ماندهٔ محاسبه‌شده | حذف با هشدار اگر تراکنش دارد |
| `categories` | دسته‌ها با **`icon`** | نگاشت نام→آیکون خودکار |
| `tags` | **برچسب‌های تراکنش** | `DEFAULT_TAGS` + `getTags` + `tagById` |
| `transactions` | تراکنش‌ها با `tag?`, `note`, `source` | هستهٔ برنامه |
| `transfers` | انتقال بین حساب‌ها | |
| `debts` | بدهی/طلب | تسویه → تراکنش |
| `installments` | اقساط | |
| `budgets` | بودجهٔ دسته‌ها | پیش‌بینی کسری |
| `payment_methods` | روش‌های پرداخت | |
| `recurring` | دوره‌ای | **روز مؤثر در ماه کوتاه** |
| `savings_goals` | اهداف | |
| `appointments` | قرارها | یادآور صوتی |
| `notes` | **یادداشت‌ها** | `cat`, `pinned` |
| `cheques` | چک‌ها | |
| `splits`, `challenges`, `currencies`, `assets`, `subscriptions` | — | |
| `activity_logs`, `telegram_users`, `trash` | سیستم | سطل ۵ تایی |

### نکات کلیدی مدل

- `rev` — شمارندهٔ نسخه برای سازش‌گیری سینک (آخرین‌نوشتن‌برنده با حفظ تراکنش‌های محلی)
- `prefs` — تم، پین، `notifyEnabled`، کلیدهای سینک و AI (حساس‌ها **هرگز به ابر نمی‌روند**)
- **مهاجرت `migrateLoadedState`** — داده‌های قدیمی: `notes=[]`, `tags=DEFAULT_TAGS`, آیکون دسته‌ها

---

## ۷. معماری سینک

```
┌─────────────┐   ذخیره/خواندن    ┌──────────────┐
│  مرورگر A   │ ◄──────────────► │ localStorage │
│  (فایننس‌پرو) │                  └──────────────┘
└──────┬──────┘
       │ ۱) باز شدن: pull + سازش‌گیری (rev)
       │ ۲) هر تغییر: push با ۳.۵ ثانیه دیباونس
       │ ۳) هر ۹۰ ثانیه: pull خودکار
       ▼
┌──────────────────────────────────┐
│  Supabase — financepro_state     │  کلید: fp-user-<نام‌کاربری>
│  ستون data = دفترکل (بدون prefs) │
│  Supabase — fp_users             │  کلید: <نام‌کاربری>
└──────────────┬───────────────────┘
       ▲       (همین جدول‌ها برای ربات تلگرام)
┌──────┴──────┐
│  مرورگر B   │  ← همان نام کاربری = همان دفترکل
└─────────────┘
```

### قواعد سینک

1. **کلید بر اساس نام کاربری** (`fp-user-<username>`) نه عدد تصادفی — همهٔ دستگاه‌های یک کاربر یک دفترکل دارند
2. **prefs هرگز به ابر نمی‌رود** — `pushToCloud` فقط `syncId` را نگه می‌دارد (پین، کلیدها، توکن‌ها امن‌اند)
3. **سازش‌گیری هنگام شروع:** ابر جدیدتر (`rev` بالاتر) → بازیابی + حفظ تراکنش‌های محلیِ سینک‌نشده (`localOnlyTx` + `mergePulledState` با dedupe بر اساس id) + توست اطلاع
4. **اگر pull خطا داد، push نمی‌کنیم** — تا دادهٔ ابر با دادهٔ قدیمی پاک نشود
5. **تنظیمات اتصال سه‌لایه:** prefs کاربر ← تنظیمات مشترک مرورگر (`fp_cloud`) ← متغیرهای محیطی Vercel (`effectivePrefs`)
6. **انتقال آفلاین:** کد Base64 (encodeState/decodeState) با مهاجرت و حفظ prefs

### کلیدهای مرورگری

| کلید | محتوا |
|---|---|
| `fp_users` | فهرست کاربران دستگاه (هش رمز) |
| `fp_session` | نشست فعلی |
| `fp_data_<userId>` | دفترکل هر کاربر |
| `fp_accent` | تم رنگی |
| `fp_theme` | روشن/تیره |
| `fp_cloud` | اتصال مشترک Supabase (برای صفحهٔ ورود) |
| `fp_smart` | وضعیت تشخیص هوشمند |

---

## ۸. PWA و اپ اندروید

### PWA (فعال روی سایت)
- `manifest.webmanifest` — نام فارسی، RTL، standalone، رنگ برند
- `sw.js` — کش آفلاین (navigate: شبکه‌اول، assets: کش‌اول) + کلیک اعلان → باز کردن برنامه
- ثبت فقط در `PROD` (جلوگیری از کش کهنه در dev)
- اعلان‌ها با `fireNotification` — ترجیحاً از SW (پس‌زمینه)

### اپ اندروید با Capacitor (پیکربندی آماده)
- `capacitor.config.ts` — appId=`ir.tajeddini.financepro`، webDir=`dist`، androidScheme=https
- `native.ts` — `isNativePlatform` / `platform` / `hasNativePlugin` (برای اثر انگشت و اعلان بومی)
- `patch-android-mirror.cjs` — میرور Aliyun برای Gradle Sync در ایران
- **راهنمای کامل:** `CAPACITOR-GUIDE.md`
- APK نهایی با Android Studio روی کامپیوتر کاربر ساخته می‌شود (VPS با ۵۰۰MB مناسب نیست)
- داده‌ها دست نمی‌خورند — اپ به همان Supabase وصل است؛ کامپیوتر فقط برای ساخت لازم است

---

## ۹. دستیار صوتی

**معماری نهایی (پس از چهار بازنویسی — باگ «برفی» PWA/اندروید):**

- **هر نشست = نمونهٔ تازهٔ `SpeechRecognition`** — عامل اصلی تکرار این بود که بعد از مکث `rec.start()` روی نمونهٔ قدیمی صدا زده می‌شد؛ کروم نتایج نشست قبل را در `results` نگه می‌دارد و جمله‌ها دوباره اضافه می‌شدند. حالا قبل از شروع، نمونهٔ قبلی `abort()` و نمونهٔ جدید ساخته می‌شود
- **بازخوانی کامل `e.results` از ایندکس ۰ در هر رویداد** — `finals`/`interim` داخل handler تعریف شده‌اند (بدون تجمع کلوژری)
- **گارد نشست یکتا (`sessionIdRef`)** — نشست‌های هم‌پوشان باطل می‌شوند؛ دو نمونه همزمان هرگز متن تحویل نمی‌دهند
- **حالت افزودنی (`baseText` + `fieldRef`)** — گفته‌های جدید به انتهای متن موجود اضافه می‌شوند؛ متن قبلی **هرگز پاک نمی‌شود**
- **حذف تکرار جمله (`appendSmart`)** — اگر جمله از قبل در انتهای فیلد بود، تکرار نمی‌شود
- **پیام‌های خطای مشخص:** رد دسترسی میکروفون / صدایی شنیده نشد / خطای شبکه (با اشاره به PWA)
- دکمه بین «دیکتهٔ صوتی» و «توقف ضبط» (قرمز تپنده) عوض می‌شود + پیش‌نمایش زندهٔ «در حال گوش دادن…»
- **۴ محل استفاده:** توضیحات تراکنش، متن یادداشت، عنوان قرار، جست‌وجوی سراسری

---

## ۱۰. تشخیص هوشمند، پارسر پیام بانکی و تگ‌ها

### تشخیص هوشمند (`detectSmart` در data.tsx)
از متن توضیح: **مبلغ** (با واحدهای هزار/میلیون/میلیارد و ارقام فارسی)، **دسته** (۱۱ گروه کلیدواژه)، **حساب** (نام حساب یا کلمهٔ کلیدی بانک)، **نوع** (درآمد/هزینه). کلید «تشخیص هوشمند» در فرم تراکنش.

### پارسر پیام بانکی (`lib/sms.ts`)
پنل «ثبت از پیام بانکی» در فرم تراکنش — تحلیل کاملاً **روی دستگاه** (هیچ‌جا فرستاده نمی‌شود).

| قابلیت | پشتیبانی |
|---|---|
| بانک ملی: `انتقال:4,509,000-` + `حساب:83008` + `مانده:` + `0531-20:40` | ✅ |
| بانک رسالت: `10.10070145.1` (ارجاع) + `-10,314,000` + `06/01_15:08` | ✅ |
| مبلغ با جداکننده + ارقام فارسی + «میلیون ریال» | ✅ |
| **تبدیل خودکار ریال→تومان** وقتی واحد ندارد (عرف بانک‌های ایران) + یادداشت هشدار | ✅ |
| علامت منفی/مثبت چسبیده (قبل یا بعد عدد) → نوع تراکنش | ✅ |
| تاریخ کوتاه بدون سال (حدس سال از امروز + اعتبارسنجی طول ماه) | ✅ |
| شمارهٔ ارجاع نقطه‌دار (شناسایی و حذف از تحلیل) | ✅ |
| نرمال‌سازی کاف/یای عربی و نیم‌فاصله | ✅ |
| استخراج مانده، ساعت، چهار رقم کارت، شمارهٔ حساب، طرف مقابل | ✅ |
| **تطبیق حساب سه‌مرحله‌ای:** کارت ← شمارهٔ حساب ← نام بانک داخل پیام | ✅ |
| حدس دسته از نام فروشنده | ✅ |
| سطح اطمینان (high/medium/low) + یادداشت‌های تحلیلی | ✅ |

### تگ‌های تراکنش
- ۴ تگ پیش‌فرض (`DEFAULT_TAGS`): ضروری، تفریحی، «میشد بعدا هم خرید»، «معمولی و قیمتش کم بود خریدم»
- **قابل مدیریت** از تب «برچسب‌ها» در مدیریت: ساخت/ویرایش/حذف با پالت ۱۰ رنگه + شمار تراکنش‌ها
- انتخاب در فرم تراکنش (چیپ‌های رنگی) + فیلتر در صفحهٔ تراکنش‌ها + ستون در اکسل
- **یادآوری هوشمند تگ‌گذاری:** بعد از ثبت هزینهٔ بدون تگ، مودال پیشنهاد می‌دهد (تگ هم‌خوان با دسته با `TAG_SUGGEST_KEYWORDS` اول می‌آید)
- **کارت تحلیل داشبورد:** توزیع تگ‌ها + «پتانسیل پس‌انداز» = همهٔ تگ‌های **غیرضروری** (با تگ‌های سفارشی کار می‌کند) + تگ‌های یتیم (حذف‌شده) در «بدون برچسب»

### گزارش هوشمند (`buildAiReport` در pages2.tsx)
گزارش متنی ۱۴ بخشی برای هوش مصنوعی: نمای کلی، روند ۶ ماه، دسته‌ها، **تگ‌ها (زنده از state)**، بودجه‌ها، بدهی‌ها، اقساط، اهداف، ثابت‌ها، دارایی‌ها، بزرگ‌ترین خرج‌ها، الگوهای رفتاری (پرخرج‌ترین روز هفته، میانگین روزانه)، سلامت مالی + پیش‌بینی، و ۵ درخواست تحلیل از AI.
- **تحلیل زنده:** streaming از Gemini (`gemini-flash-latest` از آدرس بومی گوگل — کلیدهای `AIza` و `AQ.` هر دو با پروتکل `X-goog-api-key`) / OpenRouter / OpenAI — تشخیص خودکار پروتکل از روی آدرس
- **خروجی PDF:** سند چاپی RTL (لاله‌زار/وزیرمتن) با گزارش + پاسخ AI

---

## ۱۱. تم‌های رنگی

۶ تم ترکیبی در `lib/themes.ts` + متغیرهای کامل (۱۴+ متغیر) در `index.css` با `html[data-accent]`:

| تم | accent | mint |
|---|---|---|
| زمرد و طلا (پیش‌فرض) | `#e8b04b` | `#2fb98a` |
| **زمردی دارک** (بازنویسی کامل — پس‌زمینهٔ سبز مغاکی `#030f0a`، سطوح سبز تیره، حاشیه‌های زمردی) | `#34d399` | `#5eead4` |
| زعفرانی و فیروزه | `#ff8f45` | `#14c4a8` |
| یاقوتی و صورتی | `#ff5e7e` | `#ff9db4` |
| آبی سلطنتی و آسمان | `#7aa2f7` | `#54d6e8` |
| مسی و زیتونی | `#dd9460` | `#b3c968` |

- دو راه تعویض: **دکمهٔ پالت در نوار بالا** (`AccentCycle`) و **سواچ‌های تنظیمات**
- هر تم نسخهٔ روشن هم دارد (`.light html[data-accent=…]`)
- انتخاب در `fp_accent` و `prefs.accent` ذخیره می‌شود و قبل از ورود هم اعمال می‌شود

---

## ۱۲. درس‌های فنی و نکات مهم توسعه

1. **هرگز `new Date()` مستقیم برای شمسی نه** — فقط `toJalaali`/`jalaliToISO` در `utils.ts`
2. **اعداد فارسی** — `faNum`/`faMoney`/`groupInt`، نه `toLocaleString` خالی
3. **Store مرکزی** — همهٔ تغییرات از `mutate()`؛ سینک و ذخیره خودکار می‌شود
4. **حذف‌ها فقط با `trashItem`** — سطل ۳۰ ثانیه‌ای، نوار همهٔ موارد معلق را نشان می‌دهد
5. **تم** — رنگ‌ها در `--fp-*`؛ تم جدید فقط متغیر + `themes.ts`
6. **تاریخ «امروز» باید محلی باشد** (`localISODate`) — `toISOString` بین ۰۰:۰۰ تا ۰۳:۳۰ در ایران یک روز عقب است
7. **SpeechRecognition:** هر نشست = نمونهٔ **تازه**؛ `start()` دوباره روی نمونهٔ قدیمی ممنوع (کروم نتایج قبلی را نگه می‌دارد → تکرار کلمات)
8. **دادهٔ نمونه:** برنامه با دادهٔ پایه شروع می‌شود؛ نمونهٔ کامل فقط با دکمهٔ صریح در مدیریت
9. **جدول‌های Supabase را حذف نکنید** — `financepro_state` و `fp_users` دادهٔ همهٔ کاربران‌اند
10. **`postcss.config.mjs` خالی ریشه را حذف نکنید** — سپر تنظیمات مزاحم
11. **دکمهٔ تعاملی داخل `<label>` ممنوع** — در کروم/PWA کلیک دوبار شلیک می‌شود؛ `Field` باید `div` بماند
12. **گارد همگام برای شروع‌های حساس** (`activeRef` در MicButton) — state ری‌اکت برای جلوگیری از دوبار اجرا کافی نیست
13. **بازیابی/انتقال داده همیشه با `migrateLoadedState` + حفظ `prefs`** — وگرنه اتصال ابری و جدول‌های جدید می‌پرند
14. **وقتی pull خطا می‌دهد، push نکن** — جلوگیری از پاک‌شدن دادهٔ ابر با نسخهٔ قدیمی
15. **فیلتر «سال گذشته» باید کل سال باشد** (`jalaliMonthRange(ly,1).from → (ly,12).to`) — قبلاً فقط اسفند بود
16. **ورود CSV باید اعداد فارسی را بفهمد** (`toEnDigits` + حذف جداکننده) — وگرنه مبلغ‌ها بی‌صدا صفر می‌شوند
17. **سینک چنددستگاهه هرگز با «شمارندهٔ محلی» نسخه‌بندی نمی‌شود** — شمارندهٔ هر دستگاه از صفرِ خودش شروع می‌شود و مقایسه‌اش بین دستگاه‌ها بی‌معنی است (باگ v1.18: مبلغ/حساب تراکنش‌ها خودبه‌خود برمی‌گشت). نسخه باید **مهر زمانی جهانی** (`rev = Date.now()`) باشد + ادغام **تراکنش‌محور** با `updatedAt` هر تراکنش + گارد برابری محتوا (`sameLedgerContent`) برای جلوگیری از چرخهٔ دستگاه‌های با ساعت ناهماهنگ + ارسالِ «اول بخوان، بعد تصمیم» به‌جای push کورکورانه
18. **`Forecast` در widgets یک کامپوننت JSX نیست** — تابعی است که `{ months, forecast }` برمی‌گرداند و باید به‌صورت تابعی صدا زده شود (`Forecast({ s })`)، نه `<Forecast />`. برای رندر، آن را داخل یک کامپوننت محلی (مثل `TrendChart`) صدا بزنید

---

## ۱۳. کارهای باقی‌مانده و ایده‌های آینده

### از سند تحویل اصلی
- [ ] محدود کردن RLS سوپابیس برای تولید
- [ ] سینک خودکار دوطرفهٔ کامل (الان «آخرین نوشتن برنده» + حفظ محلی‌هاست)
- [x] اپ موبایل بومی با Capacitor — **پیکربندی آماده شد (v1.15):** APK نهایی با Android Studio کاربر
- [ ] پشتیبان‌گیری خودکار روزانه در تلگرام
- [ ] دستورات بیشتر ربات (جستجو، گزارش دسته)
- [x] **ساخت ربات تلگرام مستقل (v1.24)** — `bot/bot.js` بدون وابستگی npm (فقط Node ۱۸+) با long-polling؛ دفترکل مشترک با سایت از راه جدول `financepro_state` (کلید `fp-user-<username>`)؛ فرمان‌های `موجودی`/`امروز`/`گزارش` + ثبت دستی `ثبت: …` + **فوروارد پیامک بانکی** (پارسر همان فرمت‌های ملی/ملت/رسالت). راهنمای نصب روی VPS با systemd در `VPS-GUIDE.md`. استقرار نهایی با کاربر است

### ایده‌های انجام‌شده ✅
- [x] تحلیل خودکار تگ‌ها در داشبورد (v1.14)
- [x] اتصال گزارش هوشمند به API مدل زبانی — Gemini/OpenRouter/OpenAI (v1.12)
- [x] جست‌وجوی سراسری + صوتی (v1.5)
- [x] یادآوری هوشمند تگ‌گذاری (v1.7)
- [x] پیش‌بینی کسری بودجه (v1.7)
- [x] مقایسهٔ ماهانه (v1.7)
- [x] خروجی PDF گزارش هوشمند (v1.7)
- [x] جست‌وجوی صوتی (v1.7)
- [x] حالت «روزانه» — صفحهٔ مستقل در منو (v1.7)

### ایده‌های جدید
- [ ] **خواندن خودکار پیامک بانکی — اپ همراه اندروید** (مرورگر حق SMS ندارد؛ اپ Kotlin/React-Native با SmsListener که لوکال پارس و به `financepro_state` هل بدهد)
- [ ] **ثبت با فوروارد پیامک به ربات تلگرام** — همان `parseBankSMS` سمت سرور (بدون اجازهٔ گوشی)
- [ ] قفل با اثر انگشت در اپ بومی (پلاگین Biometric + ماژول `native.ts` آماده)
- [ ] اعلان‌های بومی در اپ (Local Notifications)
- [ ] ویجت «این روز در سال‌های قبل»
- [ ] دسته‌بندی و رنگ‌بندی قرارها + تکرار قرارها (هفتگی/ماهانه)
- [ ] همگام‌سازی یادداشت‌ها با ربات تلگرام
- [ ] حالت مرور امن — محو مبالغ هنگام اسکرین‌شات
- [ ] انتشار در گوگل‌پلی (۲۵ دلار یک‌بار)
- [ ] اتصال دائمی گزارش هوشمند به مدل رایگان به‌عنوان پیش‌فرض

---

## ۱۴. لاگ تغییرات

> 📌 **قرارداد:** از این پس **هر تغییری** که در پروژه انجام می‌شود باید در این جدول ثبت شود — با شماره نسخه، شرح و فایل‌های لمس‌شده.

| نسخه | تغییر | فایل‌ها |
|---|---|---|
| v1.26 | **تقویت پچ Gradle پس از خطای `Read timed out` روی Aliyun (گزارش کاربر):** پچ چندلایه شد — ۱) میرورهای بیشتر به ترتیب اولویت: Aliyun (google/public/gradle-plugin) + **تجمیعی Huawei** (`repo.huaweicloud.com/repository/maven`) + **میرور Amazon** برای maven-central؛ درج افزایشی و بدون تکرار (فقط میرورهای غایب) ۲) timeout سریع در `gradle.properties` (`connectionTimeout=10000`, `socketTimeout=20000`) تا میرورِ قطع‌شده سریع رد شود و Gradle به بعدی برسد ۳) پشتیبانی از VPN با متغیر محیطی `FP_PROXY=host:port` که پراکسی را به `gradle.properties` می‌نویسد (راه تضمینی از ایران)؛ راهنمای عیب‌یابی `CAPACITOR-GUIDE.md` با متن دقیق خطا و فرمان VPN به‌روز شد | `patch-android-mirror.cjs`، `CAPACITOR-GUIDE.md` |
| v1.26 | **تقویت پچ Gradle برای اینترنت ایران (پاسخ به timeout میرور Aliyun):** ترتیب میرورها بازنویسی شد — Huawei (`repo.huaweicloud.com` که google+central+gradle-plugin را یکجا دارد و از ایران پایدارتر است) اول، سپس Aliyun، سپس `maven.google.com` مستقیم و Amazon؛ timeout اتصال از ۱۰ به ۸ ثانیه و سوکت از ۲۰ به ۱۵ ثانیه کاهش یافت تا میرورِ قطع‌شده سریع‌تر رد شود و Gradle به بعدی برسد. روش تضمینیِ VPN+پراکسی (`FP_PROXY`) حفظ شد. درس: از ایران ترتیب میرور و fail-fast مهم‌تر از تعداد میرور است؛ و VPN بدون وصل‌کردن پراکسی به Gradle بی‌اثر است | `patch-android-mirror.cjs` |
| v1.25 | **آماده‌سازی نهایی ساخت اپ اندروید (بدون هیچ تغییر در کد سایت — هش بیلد ثابت):** ۱) نصب پکیج‌های جاماندهٔ Capacitor v8: `@capacitor/android@8.5.0` (عامل اصلیِ شکست `cap add android` تا قبل از این) + `splash-screen@8` و `status-bar@8` — هم‌نسخه با cli/core 8.5.0 ۲) اسکریپت یک‌کلیکی `scripts/build-android.bat` (ویندوز) و `.sh`: تشخیص بار اول، `cap add android` → پچ میرور Aliyun → `npm run build` → `cap sync` → پچ مجدد (idempotent) ۳) آیکون اپ ۱۰۲۴×۱۰۲۴ تولید شد (لینک دانلود در راهنما) ۴) `CAPACITOR-GUIDE.md` به‌روز شد با وضعیت واقعی کاربر (Android Studio Quail 3 Patch 1 + SDK نصب‌شده)، متن دقیق خطای Gradle (`gradle:8.13.0` / `google-services:4.4.4`)، چک‌لیست تست اولین اجرا (۶ مورد)، و راهنمای Image Asset Studio. استقرار نهایی (اجرای اسکریپت + Android Studio) روی کامپیوتر کاربر است | `package.json` (وابستگی‌ها)، `scripts/build-android.bat`، `scripts/build-android.sh`، `CAPACITOR-GUIDE.md` |
| v1.24 | **ربات تلگرام مستقل + راهنمای VPS:** `bot/bot.js` — بدون وابستگی npm (فقط Node ۱۸+ و `fetch` داخلی)، long-polling، دفترکل مشترک با سایت از راه جدول `financepro_state` با کلید `fp-user-<username>` (دقیقاً مثل وب‌اپ)، تبدیل تاریخ جلالی→میلادی با الگوریتم دقیق jalaali-js، پارسر پیامک بانکی (ملی/ملت/رسالت)، فرمان‌های `موجودی`/`امروز`/`گزارش`/`ثبت: …`، ذخیرهٔ نگاشت chatId→username در `bot/bot-state.json`. راهنمای گام‌به‌گام نصب روی VPS (ARM64) با systemd در `VPS-GUIDE.md` (بدون pm2 به‌خاطر فضای کم) | `bot/bot.js`، `bot/package.json`، `VPS-GUIDE.md`، `.gitignore` |
| v1.0 | **نسخهٔ پایه:** ۸ صفحه (داشبورد، تراکنش‌ها با حذف ۳۰ ثانیه‌ای، گزارش دسته‌ها، بدهی‌ها، قرارها، گزارش‌ها با ExcelJS، مدیریت ۱۲ ابزار، تنظیمات)، ورود/ثبت‌نام/مهمان، قفل پین، تم روشن/تیره، تقویم شمسی، اعداد فارسی، دیکتهٔ صوتی، تشخیص هوشمند پایه، سینک Supabase | همهٔ فایل‌ها |
| v1.1 | **رفع تکرار کلمات دیکته** (بازخوانی کامل results در هر رویداد) + تغییر `Field` از label به div | `src/ui.tsx` |
| v1.2 | **حفظ متن قبلی در دیکته** (حالت افزودنی `baseText`) + گارد نشست یکتا + حذف تکرار جمله | `src/ui.tsx` |
| v1.3 | **رفع باگ منطقهٔ زمانی** (`localISODate` به‌جای `toISOString`) + **یادآوری هر قرار جدا** (`remindedIdsRef: Set`) | `src/lib/utils.ts`، `src/App.tsx` |
| v1.4 | **جلوگیری از نشت کلیدها به ابر** (فقط syncId در prefs ارسالی) + **ادغام بدون گم‌شدن تغییرات** (`localOnlyTx` + `mergePulledState`) + ورود چنددستگاهی (جدول `fp_users` + `pullUser` + بک‌فیل هنگام ورود) + متغیرهای محیطی Vercel | `src/lib/cloud.ts`، `src/lib/auth.ts`، `src/App.tsx`، `src/pages2.tsx` |
| v1.5 | **جست‌وجوی سراسری + صوتی** از نوار بالا (تراکنش/یادداشت/قرار/دسته با پرش به صفحه) | `src/App.tsx`، `src/ui.tsx` |
| v1.6 | **یادداشت‌ها:** صفحهٔ جدید با برگه‌های چسبان، دیکته، ویرایش/حذف ۳۰ ثانیه‌ای + جدول `notes` در مدل | `src/pages2.tsx`، `src/lib/data.tsx`، `src/App.tsx` |
| v1.7 | **شش قابلیت نقشهٔ راه:** ۱) یادآوری هوشمند تگ‌گذاری بعد از ثبت هزینهٔ بدون تگ ۲) پیش‌بینی کسری بودجه (هشدار ٪۸۰ + خطای عبور + ردیف‌های سه‌رنگهٔ مدیریت) ۳) مقایسهٔ ماهانه (دومیله‌ای + ٪ تغییر) ۴) خروجی PDF گزارش هوشمند ۵) جست‌وجوی صوتی ۶) **حالت «روزانه»** — صفحهٔ مستقل در منو، نه داشبورد | `src/pages.tsx`، `src/pages2.tsx`، `src/App.tsx` |
| v1.8 | **تگ‌های تراکنش:** ۴ تگ پیش‌فرض، انتخاب در فرم، فیلتر در تراکنش‌ها، ستون اکسل، مدیریت کامل تگ‌ها (تب «برچسب‌ها») + **پاسخ‌گویی کارت تحلیل داشبورد به تگ‌های سفارشی** (شناسایی «ضروری» + تگ‌های یتیم) | `src/lib/data.tsx`، `src/pages.tsx`، `src/pages2.tsx`، `src/excel.ts` |
| v1.9 | **۱۰ آیکون جدید دسته:** میوه، دانهٔ قهوه، کافه، نان، پراپ تریدینگ، قبوض، گوشت، اینترنت، مرد/زن (SVG دستی)، وام + نگاشت نام→آیکون | `src/ui.tsx`، `src/lib/data.tsx` |
| v1.10 | **ثبت تراکنش از پیام بانکی:** پنل در فرم تراکنش + پارسر اولیه (تحلیل روی دستگاه، تبدیل ریال→تومان، تطبیق حساب) | `src/lib/sms.ts` (جدید)، `src/pages.tsx` |
| v1.11 | **پارسر مطابق فرمت واقعی بانک ملی:** علامت چسبیده، تاریخ `MMDD-HH:MM` بدون سال، شمارهٔ `حساب:`، تطبیق سه‌مرحله‌ای حساب، کاف عربی + ۳ نمونهٔ واقعی | `src/lib/sms.ts`، `src/pages.tsx` |
| v1.12 | **فرمت بانک رسالت:** شمارهٔ ارجاع نقطه‌دار، مبلغ علامت‌دار تنها (`-10,314,000`)، تاریخ `MM/DD_HH:MM` + نمایش ارجاع + **اتصال هوش مصنوعی به Gemini/OpenRouter/OpenAI** با تشخیص خودکار پروتکل (کلیدهای `AIza` و `AQ.`) و انتخاب‌گر ارائه‌دهنده | `src/lib/sms.ts`، `src/pages.tsx`، `src/pages2.tsx`، `src/lib/data.tsx` |
| v1.13 | **ممیزی گزارش باگ خارجی:** دو باگ گزارش‌شده (تکرار صوتی، یادآوری تکی) از قبل رفع‌شده بودند؛ پچ‌های پیشنهادی رجوع‌به‌عقب بودند — بدون تغییر کد | — |
| v1.14 | **۱) تم «زمردی دارک»** — بازنویسی کامل ۱۴ متغیره (پس‌زمینهٔ سبز مغاکی) ۲) **فیلتر «بازهٔ دلخواه»** در ۳ صفحهٔ فیلتردار (هوک `usePeriod` + کامپوننت `PeriodFilter`؛ اکسل و چاپ هم بر اساس بازه) ۳) **تحلیل تگ‌های داشبورد** با تگ‌های سفارشی | `src/index.css`، `src/lib/utils.ts`، `src/ui.tsx`، `src/pages.tsx`، `src/pages2.tsx` |
| v1.15 | **آماده‌سازی Capacitor:** `capacitor.config.ts` (appId فارسی، وب‌دیر dist، Splash/StatusBar)، پکیج‌های core/cli/android/splash/status-bar، `assets/icon.svg`، `.gitignore` برای پوشهٔ android، `CAPACITOR-GUIDE.md` کامل، `native.ts` — بدون اثر روی بیلد سایت (خروجی بایت‌به‌بایت یکسان ماند) | `capacitor.config.ts`، `CAPACITOR-GUIDE.md`، `src/lib/native.ts`، `.gitignore`، `package.json` |
| v1.16 | **پچ Gradle برای اینترنت ایران:** `patch-android-mirror.cjs` — افزودن میرورهای Aliyun به repositoriesهای `android/build.gradle` و `settings.gradle` (idempotent، بعد از هر `cap sync`) | `patch-android-mirror.cjs` |
| v1.17 | **بازیابی کامل پروژه بعد از ریست sandbox:** همهٔ فایل‌ها از branch PR (`digital-persian-notebook-937d7`) + دانش گفتگو بازگردانده شد؛ رفع دو ناسازگاری جزئی (`Bar.delay`، امضای `mergePulledState`); بیلد سبز (۱۷۱۹KB) — بازسازی `DASTOOR-AMAL.md` و `CAPACITOR-GUIDE.md` | همهٔ فایل‌ها |
| v1.21 | **بازسازی حرفه‌ای بخش اقساط:** مدل داده از «شمارندهٔ صرف» (`paidCount`) به «برنامهٔ ماهانه» ارتقا یافت — هر قسط آرایهٔ `schedule` دارد با `{due, amount, paidAt?, txId?}` برای تک‌تک ماه‌ها (سررسید هر ماه = شروع + n ماه شمسی؛ قسط آخر مابه‌التفاوت را جذب می‌کند تا جمع دقیق شود). `normalizeInstallments` داده‌های قدیمی (فقط paidCount) را به برنامهٔ ماهانه مهاجرت می‌کند و هنگام seed/migrate صدا زده می‌شود. UI جدید: نوار خلاصه (تعداد اقساط فعال/تکمیل‌شده + مجموع قسط ماهانه + مجموع معوقات)، دکمهٔ «ثبت قسط جدید»، کارت هر قسط با نوار پیشرفت و وضعیت (تکمیل‌شده/فعال/دارای معوقه)، لیست اسکرول‌شوندهٔ برنامهٔ ماهانه که هر ماه دکمهٔ «پرداخت» (قرمز برای معوق) یا «پرداخت شد» (سبز، قابل لغو) دارد. پرداخت قسط = افزایش paidCount + پرکردن schedule + ثبت تراکنش هزینه (با دستهٔ انتخابی و حساب)؛ لغو پرداخت = فقط پاک‌کردن paidAt (تراکنش حذف نمی‌شود). فرم ثبت/ویرایش با پیش‌نمایش «هر ماه ≈ X تومان» | `src/lib/data.tsx`، `src/pages.tsx` |
| v1.23 | **گزارش دسته‌ها: پنل تراکنش‌ها زیر همان دسته (درخواست کاربر):** پنل تراکنش‌های دستهٔ انتخابی از پایین صفحه به **دقیقاً زیر کارت همان دسته** منتقل شد — رندر داخل حلقهٔ `rows.map` با `Fragment` و فراخوانی `txPanel()` بعد از دکمهٔ انتخاب‌شده. هنگام باز شدن، `scrollIntoView({block:"nearest"})` با احترام به `prefers-reduced-motion` حداقل اسکرول لازم را انجام می‌دهد. بهبودها: حاشیهٔ همرنگ دسته، `CatGlyph` در سربرگ پنل، `max-h-80` با اسکرول داخلی برای فهرست‌های طولانی، و راهنمای متنی روی هر کارت («▼ کلیک: تراکنش‌ها همین‌جا زیرش باز می‌شود» هنگام بسته / «▲ دوباره کلیک کن تا جمع شود» هنگام باز). همهٔ اطلاعات قبلی حفظ شد: بازهٔ زمانی انتخابی، جمع مبلغ، دکمهٔ بستن، تاریخ/حساب/روش پرداخت هر تراکنش | `src/pages.tsx` |
| v1.22 | **قابل‌مشاهده و قابل‌تشخیص‌کردن سینک ابری (پاسخ به «نمی‌دانم مشکل از کد است یا اینترنت»):** ۱) **رفع بن‌بست اولین سینک:** `pullFromCloud` وقتی ابر «خالی» است (ردیفی نیست) حالا `ok:true, state:undefined` برمی‌گرداند (نه خطا) — تا دستگاه تازه بتواند اولین push را بکند؛ فقط خطای واقعی (HTTP/شبکه) جلوی ارسال را می‌گیرد ۲) **نتیجهٔ push بررسی می‌شود:** `reconcile` حالا boolean برمی‌گرداند و دیباونس فقط وقتی ارسال واقعاً موفق بود، state را «فرستاده‌شده» علامت می‌زند — ارسال شکست‌خورده بعداً دوباره تلاش می‌شود ۳) **گارد اجرای همزمان** (`reconcilingRef`) جلوی تقابل پولینگ و دیباونس را می‌گیرد ۴) **نشانگر وضعیت واقعی در نوار بالا** (`SyncBadge`): سبزِ پالس‌دار = آخرین سینک موفق (با زمان)، قرمز = ناموفق (با دلیل در title + کلیک برای تلاش فوری)، خاکستری = غیرفعال؛ با رویداد `fp-sync-status` زنده به‌روز می‌شود ۵) **دکمهٔ «آزمایش اتصال» در تنظیمات:** یک GET سبک به جدول می‌فرستد و با `httpDiagnosis` دقیق می‌گوید مشکل از کلید (۴۰۱/۴۰۳)، جدول/آدرس (۴۰۴)، فرمت آدرس (۴۰۰) یا اینترنت است ۶) توست فقط هنگام «تغییر وضعیت» (موفق↔ناموفق) تا اسپم نشود | `src/lib/cloud.ts`، `src/App.tsx`، `src/pages2.tsx` |
| v1.23 | **رفع صفحهٔ خالی فرم‌های مدیریت + آیکون PWA:** ۱) **صفحهٔ خالی هنگام ثبت جدید در تب‌های دارای فیلد تاریخ** (انتقال‌ها، اهداف، چک‌ها، اشتراک‌ها): ریشه — `startNew` فیلد تاریخ را با رشتهٔ خالی مقداردهی می‌کرد و `JalaliPicker` با مقدار نامعتبر ماه NaN برمی‌گرداند که رندر تقویم را می‌شکست. اصلاح: مقدار پیش‌فرض فیلدهای تاریخ = امروز در `startNew` و `startEdit` (با اعتبارسنجی)، fallback `\|\| todayISO()` در رندر فرم، تبدیل تاریخ خالی/نامعتبر به `undefined` در `save` (نه رشتهٔ خالی)، گارد `Number.isFinite` در `JalaliPicker`، و گارد `?? []` برای آرایه‌های خالی در لیست مدیریت ۲) **آیکون PWA هنگام نصب:** ریشه — manifest فقط SVG داشت و لانچر اندروید بدون PNG نصب نمی‌کند. اصلاح: اسکریپت جدید `scripts/make-icons.mjs` (Node خالص، بدون وابستگی، encoder حداقلی PNG + پیکسل‌آرت برند: پس‌زمینهٔ سبز کاج با نمودار طلایی صعودی و نقطهٔ نعنایی) که در هر بیلد از طریق import جانبی در `vite.config.js` اجرا و `public/icon-192.png` و `public/icon-512.png` را می‌سازد (اگر موجود باشند بازسازی نمی‌شود)؛ manifest با چهار آیکون (192/512 any + 512 maskable + SVG)؛ `apple-touch-icon` و favicon پنگ در `index.html` | `src/pages2.tsx`، `src/ui.tsx`، `scripts/make-icons.mjs`، `vite.config.js`، `public/manifest.webmanifest`، `index.html` |
| v1.22 | **اقساط: مبلغ دستی + انتخاب کارت هنگام پرداخت (درخواست کاربر):** ۱) فرم ثبت/ویرایش قسط دیگر مبلغ ماهانه را **خودکار محاسبه نمی‌کند** (`Math.ceil(total/months)` حذف شد) — فیلد جدید «مبلغ هر قسط (تومان) — دستی» با `AmountInput` و اجباری در `saveInst` (`apm <= 0` → هشدار)؛ جمع‌بندی زندهٔ برنامه: `ماه‌ها × مبلغ = جمع` با نمایش رنگیِ تفاوت با مبلغ کل (دقیق ✅ / قسط آخر کمتر یا بیشتر می‌شود) + راهنمای «تقسیم مساوی» فقط به‌صورت متن پیشنهادی (بدون پرکردن خودکار) ۲) دکمهٔ «پرداخت / پرداخت معوق» در ردیف‌های برنامهٔ ماهانه دیگر مستقیم ثبت نمی‌کند — مودال جدید **«از کدام حساب؟»** باز می‌شود: لیست کارت‌ها با نام، نوع و مانده، انتخاب با تأیید بصری (حاشیهٔ طلایی + تیک)، پیش‌فرض = حساب پیش‌فرض قسط؛ `payMonth(inst, idx, accountId?)` تراکنش را روی حساب انتخابی می‌نشیند و روش پرداخت از نوع حساب حدس زده می‌شود (کارت بانکی → «کارت»، وگرنه «شبا») و توست، نام حساب را نشان می‌دهد. ماشین‌حساب وام به‌عنوان ابزار کمکیِ مستقل حفظ شد | `src/pages.tsx` |
| v1.20 | **رفع سه باگ ساختاری سینک/داده (گزارش کاربر):** ۱) **«تراکنش‌های مالِ من نیست»:** ریشه — `DataProvider` برای هر مرورگرِ بدون دادهٔ محلی (از جمله ورودِ کاربر موجود در دستگاه جدید) `seed()` با ۳۱ تراکنش نمونه می‌ساخت و `localOnlyTx` آن‌ها را هنگام ادغام به‌عنوان «تراکنش محلی» به دادهٔ واقعی اضافه می‌کرد. اصلاح: تابع جدید `emptyState()` (ساختار بدون داده) + prop جدید `fresh` برای `DataProvider` که فقط هنگام «ثبت‌نام تازه» (نشانهٔ `fp_fresh_signup` در sessionStorage + remount با کلید نسل در App) دادهٔ نمونه می‌گیرد؛ ورودِ کاربر موجود → ساختار خالی + دریافت دادهٔ واقعی از ابر ۲) **«تراکنش حذف‌شده برمی‌گردد»:** ریشه — حذف یعنی «نبودن رکورد» و ادغام نمی‌توانست آن را از «تراکنش جدیدِ محلی» تشخیص دهد، پس مرورگرِ دیگر آن را زنده می‌کرد. اصلاح: سیستم **سنگ‌قبر (Tombstone)** — `trashItem` برای تراکنش‌ها رکورد `{table, id, at}` در آرایهٔ `tombstones` (جزء state و سینک‌شونده) می‌سازد؛ `restore` مهر `updatedAt` جدید می‌زند و سنگ‌قبر را پاک می‌کند؛ `mergePulledState` سنگ‌قبرها را union و تراکنش‌هایی که سنگ‌قبرشان جدیدتر از آخرین ویرایش است را فیلتر می‌کند (بازگردانیِ عمدی با `updatedAt` جدیدتر نجات می‌یابد)؛ سطل محلی هنگام ادغام حفظ می‌شود؛ `ledgerFingerprint` شامل سنگ‌قبرها شد؛ پاک‌سازی سنگ‌قبرهای بالای ۱۸۰ روز در مهاجرت ۳) **«صفحهٔ ورود اطلاعات می‌خواهد»:** ریشه — `CloudConnectBox` وقتی اتصال نبود به‌صورت پیش‌فرض باز می‌شد. اصلاح: پیش‌فرض جمع + برچسب «(اختیاری)»؛ ورود بدون اتصال کار می‌کند | `src/lib/data.tsx`، `src/lib/cloud.ts`، `src/App.tsx` |
| v1.19 | **بازگردانی کامل `src/pages2.tsx` به نسخهٔ کامل (رفع حالت هیبریدی):** همهٔ بخش‌ها به نسخهٔ نهایی ارتقا یافتند — `AppointmentsPage` (یادآور صوتی + انتخابگر ساعت جدولی `TimeWheel` + دکمه‌های پررنگ ویرایش/حذف + خروجی ICS)، `ReportsPage` (گزارش هوشمند ۱۴ بخشی + تحلیل زنده با هوش مصنوعی Gemini/OpenRouter/OpenAI + نمودار مقایسهٔ ماهانه + فیلتر بازهٔ دلخواه + خروجی PDF گزارش+تحلیل + نمودار روند با کامپوننت محلی `TrendChart` چون `Forecast` کامپوننت JSX نیست)، `ManagePage` (تب برچسب‌های قابل‌ساخت/ویرایش/حذف + انتخاب‌گر آیکون دسته + تأیید حذف حساب با شمارش تراکنش + رنگ‌بندی مصرف بودجه + ۱۲ ابزار CRUD)، `SettingsPage` (سینک امن v1.18 + تنظیمات هوش مصنوعی + انتخاب‌گر ارائه‌دهنده + تم‌های ترکیبی + پین + انتقال/پشتیبان با مهاجرت)، به‌همراه حفظ عینِ `NotesPage` و `DailyPage`. بیلد سبز (۱۷۴۴KB) — publish از این محیط امن شد | `src/pages2.tsx` |
| v1.18 | **رفع باگ «عوض‌شدن خودبه‌خود مبلغ/حساب تراکنش‌ها» در سینک چنددستگاهه:** ریشه: `rev` شمارندهٔ محلیِ هر دستگاه بود و بین دستگاه‌ها قابل مقایسه نبود + ادغام، کورکورانه نسخهٔ ابر را برنده می‌کرد + ارسالِ دیباونس‌شده کورکورانه نسخهٔ قدیمی را روی ابر می‌نوشت. اصلاح‌ها: ۱) `rev` به مهر زمانی جهانی (`Date.now()`) تبدیل شد ۲) فیلد `updatedAt` به `Tx` اضافه و در `mutate` با مقایسهٔ محتوایی (بدون خود مهر) روی تراکنش‌های واقعاً تغییرکرده ثبت می‌شود ۳) `mergePulledState` حالا تراکنش‌محور است: برای idهای مشترک، نسخهٔ جدیدترِ هر تراکنش (بر اساس updatedAt/createdAt) برنده می‌شود و در تساوی، محلی می‌ماند ۴) `sameLedgerContent` (اثرانگشت ۱۸ جدول معنادار) جلوی چرخهٔ بی‌پایان ادغام/لاگ بین دستگاه‌های با ساعت ناهماهنگ را می‌گیرد ۵) مسیر دیباونس در `App.tsx` به‌جای push کورکورانه، `reconcile()` می‌کند (اول بخوان، بعد تصمیم) ۶) `doSync` دستی در تنظیمات هم به همان منطق امن (rev + merge تراکنش‌محور + گارد برابری) تبدیل شد — قبلاً با مقایسهٔ `lastSync` و `Object.assign` کل‌نگر، داده را له می‌کرد ۷) بازیابی پشتیبان و کد انتقال هم `prefs` محلی را حفظ و `migrateLoadedState` را اجرا می‌کنند. **درس:** برای سینک چنددستگاهه هرگز از شمارندهٔ محلی به‌عنوان نسخه استفاده نکن — مهر زمانی یا نسخهٔ سمت سرور لازم است | `src/lib/data.tsx`، `src/lib/cloud.ts`، `src/App.tsx`، `src/pages2.tsx` |

---

*این سند همراه پروژه نگه داشته می‌شود و با **هر تغییر** باید به‌روز شود (بخش ۱۴).*
*فایننس‌پرو — ساخته‌شده با ❤️ برای کاربران فارسی‌زبان*
