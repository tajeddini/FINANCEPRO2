# 📦 سند تحویل پروژه — فایننس‌پرو (FinancePro)

> دفترکل دیجیتال شخصی برای کاربران فارسی‌زبان — PWA با تقویم شمسی، اعداد فارسی، ربات تلگرام، سینک ابری و اپ اندروید

این سند مرجع دائمی پروژه است و با **هر تغییر** باید به‌روز شود (بخش لاگ تغییرات در انتها).

---

## ۱. معرفی و اصول حاکم

فایننس‌پرو یک وب‌اپلیکیشن مدیریت مالی شخصی برای کاربران ایرانی است. اصول حاکم:

- **کاملاً راست‌به‌چپ (RTL)** و فارسی — فونت Lalezar (تیتر) + Vazirmatn (متن)
- **تقویم شمسی (جلالی)** در همه‌جا — ذخیره به میلادی، نمایش به شمسی (با jalaali-js)
- **اعداد فارسی** با جداکنندهٔ هزارگان — از `faNum()` و `faMoney()` استفاده شود، نه `toLocaleString` خالی
- **چندکاربره** — هر کاربر دادهٔ کاملاً جدا دارد + حالت مهمان
- **PWA** — نصب روی موبایل و دسکتاپ، کارکرد آفلاین
- **سینک ابری Supabase** — بین دستگاه‌ها، با کلید مشترک `fp-user-<نام‌کاربری>`
- **حذف امن** — سطل زبالهٔ ۳۰ ثانیه‌ای با قابلیت بازگشت

---

## ۲. پشتهٔ فناوری

| حوزه | فناوری |
|---|---|
| فریم‌ورک | React 18 + Vite 6 |
| زبان | TypeScript (strict) |
| استایل | Tailwind CSS v4 با CSS Variables برای تم |
| فونت | Lalezar + Vazirmatn |
| تاریخ شمسی | jalaali-js |
| نمودار | Recharts |
| آیکن | Lucide React |
| اکسل | ExcelJS |
| بک‌اند/دیتابیس | Supabase (PostgreSQL + REST + Realtime) |
| میزبانی سایت | Vercel |
| ربات تلگرام | Node.js بدون وابستگی (Polling روی VPS) |
| اپ اندروید | Capacitor |

---

## ۳. ساختار فایل‌ها

```
financepro/
├── index.html                ← ورودی HTML (RTL، فونت‌ها، PWA)
├── capacitor.config.ts       ← پیکربندی اپ اندروید
├── patch-android-mirror.cjs  ← پچ Gradle برای اینترنت ایران
├── src/
│   ├── main.tsx              ← بوت‌استرپ + ثبت Service Worker
│   ├── App.tsx               ← پوسته: مسیریابی، ورود، جست‌وجو، یادآوری، نوار بازگشت
│   ├── index.css             ← سیستم طراحی: همهٔ تم‌ها + کامپوننت‌ها
│   ├── ui.tsx                ← کتابخانه UI: توست، مودال، تقویم، دستیار صوتی، فیلتر بازه
│   ├── pages.tsx             ← داشبورد، تراکنش‌ها، گزارش دسته‌ها، بدهی‌ها و اقساط
│   ├── pages2.tsx            ← قرارها، گزارش‌ها+AI، مدیریت، تنظیمات، یادداشت‌ها، روزانه
│   ├── widgets.tsx           ← نقشه حرارتی، پیش‌بینی، امتیاز سلامت، نشان‌ها
│   ├── excel.ts              ← خروجی اکسل چندبرگی + CSV + ICS
│   └── lib/
│       ├── data.tsx          ← مدل داده + Store مرکزی + تگ‌ها + سنگ‌قبر + اقساط
│       ├── auth.ts           ← احراز هویت چندکاربره
│       ├── cloud.ts          ← سینک Supabase + ادغام چنددستگاهه
│       ├── utils.ts          ← تقویم شمسی + اعداد فارسی + بازه‌ها + اعلان
│       ├── sms.ts            ← پارسر پیام‌های بانکی (ملی/ملت/رسالت)
│       ├── themes.ts         ← تعریف تم‌های رنگی
│       └── native.ts         ← تشخیص پلتفرم (وب/بومی)
├── bot/                      ← ربات تلگرام (بدون وابستگی)
│   ├── bot.js
│   └── package.json
├── scripts/                  ← اسکریپت‌های ساخت اپ اندروید
├── public/                   ← PWA: manifest + sw.js + icon.svg
├── CAPACITOR-GUIDE.md        ← راهنمای ساخت اپ اندروید
└── VPS-GUIDE.md              ← راهنمای ربات تلگرام روی VPS
```

---

## ۴. فهرست صفحه‌ها و قابلیت‌ها

| صفحه | قابلیت‌های کلیدی |
|---|---|
| **داشبورد** | موجودی/درآمد/هزینه با مخفی‌سازی جدا، ثبت سریع (صوتی + تشخیص هوشمند)، حساب‌ها، فعالیت اخیر، تحلیل رفتار خرج با تگ‌ها |
| **روزانه** | خرج و درآمد امروز، برنامهٔ امروز با تیک، یادآوری‌های هفتهٔ پیشِ رو |
| **تراکنش‌ها** | فیلتر نوع + بازهٔ زمانی (۹ آماده + دلخواه)، تگ‌ها، ویرایش، حذف با بازگشت ۳۰ ثانیه، ورود CSV، ثبت از پیام بانکی |
| **گزارش دسته‌ها** | دونات + نوارها، کلیک روی دسته → تراکنش‌هایش زیر همان کارت |
| **بدهی‌ها** | بدهی/طلب/اقساط، تسویه با ثبت تراکنش، ماشین‌حساب وام، QR درخواست وجه |
| **قرارها** | تقویم شمسی، ساعت زنده ۲۴ساعته، انتخابگر ساعت، تایپ صوتی، یادآور صوتی سرِ ساعت، ICS |
| **یادداشت‌ها** | کارت‌های رنگی، دسته‌بندی، سنجاق، تایپ صوتی، ویرایش/حذف |
| **گزارش‌ها** | نمودار ۶ ماهه، پیش‌بینی، امتیاز سلامت، نقشه حرارتی، نشان‌ها، مقایسه ماهانه، **گزارش هوشمند + تحلیل با هوش مصنوعی**، اکسل/PDF |
| **مدیریت** | ۱۲ ابزار CRUD: حساب، دسته (با آیکون)، تگ، انتقال، دوره‌ای، هدف، بودجه، روش پرداخت، چک، اشتراک، دارایی، ارز + داده نمونه/پاک‌سازی |
| **تنظیمات** | تم (۶ رنگی + شیشه‌ای تیره)، پین، سینک ابری + تست اتصال، هوش مصنوعی، ربات تلگرام، پشتیبان/بازیابی، انتقال بین مرورگر، حساب |

---

## ۵. معماری سینک ابری (مهم)

- **کلید مشترک:** هر کاربر یک ردیف در جدول `financepro_state` با id = `fp-user-<نام‌کاربری>` دارد.
- **نسخه‌بندی:** فیلد `rev` مهر زمانی جهانی است (`Date.now()`) — نه شمارندهٔ محلی. این ریشهٔ باگ «عوض‌شدن خودبه‌خود تراکنش‌ها» را خشکاند.
- **ادغام تراکنش‌محور:** هنگام pull، برای هر تراکنشِ مشترک، نسخهٔ جدیدتر (بر اساس `updatedAt`) برنده است.
- **سنگ‌قبر (Tombstone):** حذف تراکنش یک رکورد `{id, at}` در `tombstones` می‌سازد که سینک می‌شود تا حذف در همهٔ دستگاه‌ها ماندگار بماند.
- **امنیت:** `prefs` حساس (پین، کلیدها، توکن‌ها) **هرگز** به ابر فرستاده نمی‌شود — فقط `syncId`.
- **تست اتصال:** دکمهٔ «آزمایش اتصال» در تنظیمات، جدول را ping می‌کند و خطاها را با راهنمای عملی نشان می‌دهد.

### جدول‌های Supabase

```sql
create table if not exists public.financepro_state (
  id         text primary key,
  data       text,
  updated_at timestamptz default now()
);
alter table public.financepro_state enable row level security;
create policy "open_anon" on public.financepro_state for all using (true) with check (true);

create table if not exists public.fp_users (
  username   text primary key,
  data       text,
  updated_at timestamptz default now()
);
alter table public.fp_users enable row level security;
create policy "open_anon" on public.fp_users for all using (true) with check (true);
```

---

## ۶. تم‌های رنگی

۶ تم ترکیبی + تم شیشه‌ای تیره (Glassmorphism Dark)، همگی با CSS Variables (`--fp-*`) و انتخاب از تنظیمات یا دکمهٔ پالت در نوار بالا:
- زمرد و طلا (پیش‌فرض) · زعفرانی و فیروزه · یاقوتی و صورتی · آبی سلطنتی · مسی و زیتونی · **شیشه‌ای تیره**

---

## ۷. درس‌های فنی (هرگز نشکنید)

1. تاریخ شمسی فقط با `toJalaali()`/`jalaliToISO()` — هرگز `new Date()` مستقیم.
2. اعداد فارسی با `faNum()`/`faMoney()`.
3. همهٔ تغییرات داده از `mutate()` در data.tsx بگذرد.
4. حذف با `trashItem()` (سطل ۳۰ ثانیه)، نه حذف مستقیم.
5. رنگ‌ها فقط از `--fp-*`.
6. سینک با مهر زمانی جهانی (`rev = Date.now()`)، نه شمارندهٔ محلی.
7. `prefs` حساس هرگز به ابر نرود.
8. حذف تراکنش باید سنگ‌قبر بسازد تا ماندگار بماند.
9. `Field` از `div` ساخته شود نه `label` (دکمهٔ صوتی داخل label دوبار شلیک می‌کند).
10. دستیار صوتی: بازسازی متن از صفر + حذف تکرار (idempotent)، حلقهٔ restart ممنوع.
11. Gradle از ایران فقط با میرور + (در صورت نیاز) پروکسی صریح کار می‌کند.

---

## ۸. نقشهٔ راه

### انجام‌شده ✅
- [x] اپ وب کامل (۱۰ صفحه) + PWA
- [x] سینک ابری Supabase چنددستگاهه امن
- [x] ربات تلگرام مستقل (bot/bot.js)
- [x] پیکربندی اپ اندروید (Capacitor)
- [x] گزارش هوشمند + تحلیل با هوش مصنوعی
- [x] تم شیشه‌ای تیره

### ایده‌های آینده
- [ ] محدود کردن RLS سوپابیس برای تولید
- [ ] سینک دوطرفهٔ کامل (الان «آخرین نوشتن برنده»)
- [ ] انتشار اپ اندروید در گوگل‌پلی
- [ ] قفل اثر انگشت در اپ (Capacitor Biometric)
- [ ] اتصال گزارش هوشمند به API مدل زبانی به‌صورت مستقیم‌تر
- [ ] جست‌وجوی سراسری صوتی پیشرفته

---

## ۹. لاگ تغییرات

| نسخه | تغییر |
|---|---|
| v1.0 | نسخهٔ اولیه — ۸ صفحه، تقویم شمسی، اعداد فارسی، PWA |
| v1.2 | رفع تکرار کلمات در دستیار صوتی (معماری idempotent) |
| v1.3 | اصلاح منطقهٔ زمانی (تاریخ محلی به‌جای UTC) |
| v1.10 | ثبت تراکنش از پیام بانکی (پارسر SMS) |
| v1.18 | رفع باگ «عوض‌شدن خودبه‌خود تراکنش‌ها» — سینک با مهر زمانی جهانی + ادغام تراکنش‌محور + سنگ‌قبر |
| v1.19 | بازگردانی کامل پروژه، پارسر بانک رسالت |
| v1.20 | جداسازی دادهٔ نمونه از ورود کاربر موجود + اتصال ابری اختیاری |
| v1.22 | اقساط با مبلغ دستی + انتخاب حساب هنگام پرداخت |
| v1.23 | گزارش دسته‌ها: تراکنش‌ها زیر همان کارت |
| v1.24 | ربات تلگرام مستقل + راهنمای VPS |
| v1.27 | پچ Gradle برای اینترنت ایران (میرور + پروکسی خودکار) |
| — | تم شیشه‌ای تیره (Glassmorphism Dark) |
| v1.28 | **همگام‌سازی سایت با سند (رفع کمبودها):** افزودن «تحلیل رفتار خرج با تگ‌ها» به داشبورد (نوارهای رنگی بر اساس برچسب + پتانسیل پس‌انداز)؛ افزودن «مقایسهٔ ماهانه» و «گزارش هوشمند برای هوش مصنوعی» به گزارش‌ها؛ افزودن سه بخش «هوش مصنوعی»، «ربات تلگرام» و «انتقال بین مرورگر» به تنظیمات — مطابق بخش ۴ سند |
| v1.29 | **چیدمان تک‌خطی ردیف تراکنش:** قیمت به سمت چپ منتقل شد؛ همهٔ اطلاعات (دسته + برچسب + توضیح + حساب + روش پرداخت) روی یک خط خوانا با truncate؛ دکمه‌های ویرایش/حذف به‌صورت آیکون‌دار کوچک و **همیشه‌نما** (رفع پنهان‌ماندن با هاور — در موبایل هم دیده می‌شوند) |

---

*این سند همراه پروژه نگه داشته می‌شود و با هر تغییر بزرگ به‌روز می‌شود. — فایننس‌پرو*
